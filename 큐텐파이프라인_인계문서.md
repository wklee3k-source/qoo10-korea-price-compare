# 큐텐재팬 한국화장품 파이프라인 — 인계 문서

**버전 v4.2.0** · 갱신 2026-07-28 · 저장소 `wklee3k-source/qoo10-korea-price-compare`

---

## 1. 운영 방침

**양보다 질. 하루 100건 검수.** 속도보다 정확도·안정성 우선.

하루 일과:

```
02:00  발굴 시작 (외부 크론)
       → 500건 쌓이면 자동 통합 → 번역요청_01~03.md 생성
낮     번역 (파일 통째로 복사 → 다른 Claude 창에 붙여넣기)
       → 반영 → 검증
저녁   검수 100건
```

---

## 2. 프로세스

```
수확 ──(상품명을 발굴 검색어로 공급, 수동 실행)──┐
                                                 ↓
                        발굴 → (500건) → 통합 → 번역요청서 자동생성
                                                     ↓
                                     (사용자가 다른 Claude 창에서 번역)
                                                     ↓
                                           반영 → 검증 → 검수페이지
                                                              ↓
                                        검수 확정 → 브랜드·표기 사전 자동학습
```

수확 상품 자체는 검수 대상이 아니라 **발굴 검색어 재료**다.

---

## 3. 현재 데이터 (2026-07-28)

| 항목 | 수량 |
|---|---:|
| 발굴 상점 | 9,107곳 |
| 발굴 상품(통합본) | 4,586건 |
| 번역 완료 | 4,586건 (100%) |
| 검증 | **전체 재검증 진행 중** |
| 수확 상품 | 67,583건 (상점 700곳 / 미수확 8,407곳) |
| 검색어 재고 | 26,507개 (약 50일치) |
| 검수페이지 | 845건 (재검증 동안 동결) |

검수페이지: **https://wklee3k-source.github.io/qoo10-korea-price-compare/**

**백업 2종** (`hwahae-live` 브랜치 `output/archive/`)
- `hwahae_verified_before_full_reverify.json` 3,478건 — 재검증 이전 원본
- `hwahae_verified_v39_partial.json` 3,740건 — v4.0.0 이전 코드로 재검증한 분량

두 파일과 새 결과를 비교하면 링크 회수 효과를 같은 상품 기준으로 실측할 수 있다.

---

## 4. 브랜치 구조 (절대 섞지 말 것)

| 용도 | 브랜치 | 주요 파일 |
|---|---|---|
| 발굴·통합·번역 | `discovery-live` | `discovery_state.json`, `discovery_state_0~1.json`, `번역요청_NN.md`, `merge_baseline.json` |
| 수확 | `fullcatalog-live` | `fullcatalog_state_0~5.json` |
| 검증 | `hwahae-live` | `hwahae_verified_0~5.json`, `hwahae_verified_39.json`, `archive/` |
| 검수페이지 | `main` 의 `docs/` | `index.html`, `review_NN.html` |
| 검수 결정 | `main` 의 `comparison/decisions/` | 사진 선택 결과 |

---

## 5. 워커 수

| 단계 | 워커 |
|---|---:|
| 발굴 | 2 |
| 수확 | 6 |
| 검증 | **6** |

⚠️ 워커 수를 바꾸면 **샤딩 나눗셈(`% N`)도 반드시 같이** 고칠 것. 안 그러면
남는 나머지값 항목이 어느 워커에도 배정되지 않아 영구 미처리된다.
`simulation.py` 항목 40·43이 자동 검사한다.

---

## 6. 외부 크론 (cron-job.org) — 작업 2개

```
URL    : https://api.github.com/repos/wklee3k-source/qoo10-korea-price-compare/actions/workflows/qoo10-pipeline.yml/dispatches
Method : POST
Header : Authorization: token <PAT> / Accept: application/vnd.github+json
```

**① 매일 02:00 — 발굴**
```json
{"ref":"main","inputs":{"run_discovery":"true","discovery_parallel":"true"}}
```

**② 4시간마다(00:10, 04:10 …) — 검증 + 안전망**
```json
{"ref":"main","inputs":{"run_hwahae_verify":"true","run_merge_discovery":"true"}}
```

- `run_merge_discovery`를 ②에 넣는 이유: 발굴 안전망(`ensure_discovery_alive`)이
  통합 트리거에 딸려서만 돈다. 빼면 "발굴이 죽으면 통합이 안 불리고 →
  안전망도 안 돌아 영영 죽은 채"가 된다.
- 시각을 정각이 아니라 10분으로 어긋나게 둔다.
- 검증 타임아웃 220분 = 크론 4시간과 20분 여유(큐 적체 방지).

**수동 실행 (크론에 넣지 않음)**
```json
수확     {"ref":"main","inputs":{"run_harvest_catalog":"true"}}
사전학습 {"ref":"main","inputs":{"run_learn_decisions":"true"}}   ← 검수 저장 시 자동 실행됨
```

⚠️ **수확은 반드시 발굴과 다른 트리거로.** 통합의 일시정지가 run 전체를
취소하므로, 같은 트리거로 켜면 발굴을 세우려다 수확까지 죽는다.

---

## 7. 번역 방법 (붙여넣기 1회)

```
통합 완료 → discovery-live 에 output/번역요청_01.md, _02.md ... 자동 생성
          → 파일 전체를 복사해 다른 Claude 창에 붙여넣기 (따로 쓸 말 없음)
          → 응답을 python src/import_translation_response.py 로 반영
```

- 지시문·성분어 용어집(`ドクダミ→어성초`, `シカ→시카`)·출력형식이 파일 안에 있다.
- **200줄씩 분할.** 500줄을 한 장에 주면 응답이 약 27k 토큰이라 잘리거나
  번호가 어긋난다(과거 API 번역에서 응답이 잘려 2,035건이 일본어인 채 굳은 사고).
- **키는 순번이 아니라 상품번호.** 안전망 통합이 4시간마다 돌아 요청서가
  새로 생성되면 순번이 밀려 엉뚱한 상품에 이름이 박힌다.
- 안전장치: 이미 번역된 건 덮어쓰지 않음 / 빈칸·가나 잔존값은 건너뜀.
- 엑셀(`미번역상품.xlsx`) 경로도 그대로 남아 있다.

---

## 8. 검증 구조 (6소스)

| 소스 | 종류 | 무료 한도 | 역할 |
|---|---|---|---|
| 화해 | 상품DB | 스크래핑 | 승자 75% |
| 무신사 | 상품DB | 스크래핑 | |
| 네이버쇼핑 | 상품DB | 25,000/일 | **구매링크 98%가 여기서** |
| 다음 웹문서 | 제목 | 30,000/일 | 이름 확인 |
| 네이버 웹문서 | 제목 | 25,000/일(쇼핑과 공유) | 이름 확인 |
| Exa | 제목 | **1,428/월** | 보조 호출만 |

**채택 규칙**
- 2곳 이상 합의 + **상품DB 최소 1곳 필수**(웹문서 소스만으로는 성립 불가)
- 예외: 네이버가 직접 찾고 **구매링크까지 확보**하면 단독 통과(v4.1.0).
  `single_source_naver` 표시가 남고 검수페이지에서 경고 + 뒤쪽 배치.
- 네이버가 링크를 못 주면 다른 소스가 확인한 이름으로 **네이버 재검색**(v4.0.0).
  표본 100건 회수율 71%. `naver_rematched` 표시가 남는다.
- Exa는 무료 3곳이 정족수를 채웠고 화해도 찾은 경우 생략(크레딧 절약).
  401/402/403은 재시도하지 않고 그 소스만 끈다.

---

## 9. 검수페이지

- **브랜드 확실도 순 정렬**: 일치 → 판단불가 → 불일치. 같은 등급 안에서는
  기존 순서 보존(매번 뒤바뀌면 어제 어디까지 봤는지 알 수 없다).
- 단독 매칭(`single_source_naver`)은 같은 등급 안에서 뒤로.
- 경고 배지: 브랜드 미확인 / 브랜드 불일치 / 재검색 매칭 / 단독 매칭.
- **사진 선택 = 같은 상품 확정.** 저장하면 `comparison/decisions/`에 커밋되고,
  그 push가 사전 학습을 자동 실행한다(v3.8.0).

**검수페이지 동결**: `hwahae-live`에 `output/.hold_review` 파일이 있으면
생성·배포를 건너뛴다. 재검증처럼 통합본을 비우는 동안 페이지가 덮어써지는
것을 막는다. **재검증이 끝나면 이 파일을 지울 것.**

---

## 10. 검수 피드백 루프

확정(사진 선택) 1건에서 두 가지를 뽑아 사전에 되먹인다.

| 산출물 | 파일 | 현재 |
|---|---|---|
| 브랜드 대응(일본어→한글) | `data/brand_translations_learned.json` | 커버리지 38.6% |
| 표기 변형(UV=유브이 등) | `data/name_alias_learned.json` | 신규 |

**오염 방지**: 확정+비제외 건만 사용 / 기존 항목 덮어쓰기 금지 / 표기 변형은
나머지가 거의 일치하고 딱 한 토큰씩만 다를 때만 채택 / 가나 잔존값 차단.

사전이 오염되면 이후 모든 판정에 퍼지고 추적이 어렵다. 그래서 수확량이
적더라도(1,000건당 6개) 보수적으로 잡았다.

---

## 11. 실측으로 확인된 함정

| # | 내용 |
|---|---|
| ① | 같은 브랜치 동시 쓰기 = 40배 느려짐 |
| ② | 일시정지 로직은 **run 전체**를 취소 — 발굴·수확을 같은 트리거로 켜면 같이 죽음 |
| ③ | 검수페이지는 검증본의 `translated_kr`을 우선 사용 |
| ④ | `.git` 비대화(2.7GB 사고) — `--depth 1` 얕은 클론만 |
| ⑤ | 시드 분배는 최초 1회뿐이었음 → 고갈된 워커 영구 정지 (v3.1.0에서 자동보충으로 해결) |
| ⑥ | push 경합 시 `git reset --hard`가 결과·입력 파일을 지움 → 백업은 반드시 push **전에** (v3.1.1~3.1.2) |
| ⑦ | 소스 하나가 결제/인증 오류로 죽으면 상품이 '보류'로 쌓여 검증 전체가 멈춤 → 그 소스만 끄고 진행 |
| ⑧ | YAML은 같은 키(`push:`)가 두 번 나오면 앞엣것을 조용히 버림 |
| ⑨ | 워커수 변경 시 낡은 샤드 파일이 남아 조기 완료·중복 검증 → 승계는 매번 재샤딩 |

---

## 12. 자동화된 것

- **자동통합**: 발굴이 500건 쌓이면 통합 자동 호출(`merge_threshold`, 0이면 끔).
  기준선은 `merge_baseline.json`. 진행중 통합이 있으면 중복 트리거 안 함.
- **검색어 자동보충**: 워커의 pending이 0이 되면 수확본에서 새 검색어를 채움.
  배정은 `md5 % 워커수` 결정적 해시라 워커끼리 겹치지 않는다.
- **사전 학습**: 검수 저장 push가 곧 트리거.

---

## 12-B. 판단 근거가 남는 자료 (전부 저장소에 있다)

사람이 확인한 것은 반드시 파일로 남긴다. 남기지 않으면 다음 회차에 같은
것을 또 확인하게 된다. 아래 자료는 모두 검수페이지 생성 때 자동으로 읽힌다.

| 파일 | 내용 | 규모 |
|---|---|---:|
| `data/brand_translations_learned.json` | 큐텐 브랜드 → 한글 브랜드 | 1,114 |
| `data/foreign_brands.json` | 국내 유통 없는 브랜드(제외) | 34 |
| `data/manual_exclusions.json` | 육안 확인한 오매칭(제외) | 448 |
| `data/collected_pages.json` | 로컬 크롬 수집 판매페이지 정보 | 2,465 |
| `data/name_alias_learned.json` | 표기 변형 사전 | — |
| `output/collected_pages.json` (hwahae-live) | 수집 원본 | 2,572 |

**`collected_pages.json` 이 특히 중요하다.** 검증 데이터의 brand 칸은
판매처가 자기 스토어명을 넣는 경우가 많아 신뢰도가 낮은데, 여기 담긴
brand 는 상품 페이지에서 직접 읽은 값이다. 이걸로 대조해 오매칭 187건을
찾아냈다(나이키 운동화·후지필름 카메라·프라모델까지 검수 목록에 있었다).

새로 발굴된 상품은 이 자료에 없으므로, 로컬 수집기를 다시 돌려 채운 뒤
`data/collected_pages.json` 을 갱신해야 한다.

---

## 13. 주요 파일

| 파일 | 역할 |
|---|---|
| `src/iterative_low_review_discovery.py` | 발굴 (리뷰<10 필터, 검색어 길이별 통계) |
| `src/refill_discovery_keywords.py` | 검색어 고갈 시 수확본에서 자동 보충 |
| `src/harvest_full_catalog.py` | 수확 |
| `src/merge_discovery_states.py` | 발굴 통합 (번역 보존 필수) |
| `src/export_translation_request.py` | 번역요청서 생성 (200줄 분할) |
| `src/import_translation_response.py` | 번역 응답 반영 |
| `src/hwahae_verify_batch.py` | 검증 (6소스) |
| `src/naver_rematch_probe.py` | 실험: 재검색 회수율 측정(읽기 전용) |
| `src/learn_from_decisions.py` | 검수 확정 → 사전 학습 |
| `src/build_review_batches.py` | 검수페이지 생성 |
| `simulation.py` | **회귀검사 48항목 — 배포 전 반드시 통과** |
| `결정대기목록.md` | 미결 항목 (현재 0건) |

---

## 14. 작업 원칙

0. **코드 수정은 한 창에서만 한다.** 여러 창에서 같은 파일을 고치면 한쪽이
   조용히 덮어써진다. 실제로 2026-07-29에 같은 버전 번호(v4.4.0)로 서로
   다른 내용의 커밋 두 개가 생겼고, 리베이스로 겨우 합쳤다. 타이밍이
   어긋났으면 한쪽 작업이 그대로 사라졌을 것이다.
   다른 창에서는 조회·측정·번역만 한다.

1. 과거 실패 목록 점검 → 같은 패턴이 없는지 확인
2. 작업 실행
3. 새 실패 발생 시 → 목록 추가 + `simulation.py` 검사 항목 추가
4. `simulation.py` 통과 후 배포
5. 버전 `x.x.x` — 첫째=신규/추가생성, 둘째=기능추가, 셋째=버그수정

---

## 15. 검토했으나 도입하지 않은 것

| 아이디어 | 판단 |
|---|---|
| 소스 간 이름 일치로 자동 거부 | **보류.** 기계 오탐 21%. `UV`↔`유브이` 같은 정상 매칭까지 버림 |
| 브랜드 불일치 자동 거부 | **보류.** 사전 커버리지 38.6%뿐이라 지금 걸면 34%가 통째로 버려짐 |
| 가격 이상치 탐지 | 도입 안 함 |
| 용량 대조 게이트 | 배지·자동수정이 이미 있어 추가 가치 작음 |
| 신뢰 판매처 필터 완화 | 현행 유지 |

공통 원칙: **자동으로 버리지 않는다.** 기계가 걸러낸 것 중 상당수가 정상
매칭이고, 한번 버리면 버려졌다는 사실조차 안 보인다. 대신 표시하고 사람이 본다.
