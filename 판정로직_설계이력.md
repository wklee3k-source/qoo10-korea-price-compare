# 판정 로직 설계 이력

새 판정 규칙을 만들기 전에 이 문서를 먼저 읽는다. 여기 적힌 것들은 전부
**실제로 시도했다가 실패했거나, 실패를 겪은 뒤에 고쳐 만든 것**이다.
같은 함정을 다시 밟지 않으려고 남긴다.

커밋 메시지에도 근거를 적어두지만, 커밋은 시간순이라 "왜 이 조건이
이렇게 좁은가"를 찾으려면 수십 개를 뒤져야 한다. 이 문서는 그 이유만
모아둔 것이다.

---

## 1. 반복해서 확인된 원칙

### 1-1. 자동으로 버리지 않는다

기계가 걸러낸 것 중 상당수가 정상 매칭이다. 한번 버리면 **버려졌다는
사실조차 보이지 않는다.** 그래서 규칙은 등급을 내리거나 표시를 붙이는
쪽으로 쓰고, 목록에서 제거하는 건 사람이 눈으로 확인한 것만 한다.

- 자동 규칙 → `build_review.py` 코드
- 사람 판단 → `data/manual_exclusions.json` (452건)

두 경로를 섞지 않는다. 사람 판단을 코드로 일반화하려다 정상 매칭이
대량으로 사라진 사례가 아래에 여러 건 있다.

### 1-2. 규칙을 넓히기 전에 반드시 영향 건수를 잰다

"이 조건이면 잡히겠다" 싶어도 실제로 재보면 오탐이 훨씬 많은 경우가
대부분이었다. 재보지 않고 넣었다가 되돌린 적이 여러 번 있다.

### 1-3. 판단 근거가 사람이면 데이터로 남긴다

한 번 확인한 것을 다음 회차에 또 확인하지 않으려면 파일로 남아야 한다.
`brand_translations_learned.json`, `foreign_brands.json`,
`manual_exclusions.json`, `collected_pages.json` 이 그 역할이다.

---

## 2. 시도했다가 못 쓴 방법들

### 2-1. 제형이 두 개 이상이면 세트 (v7.20.0에서 기각)

실측 255건이 걸렸는데 그중 **A등급 86건이 멀쩡한 단품**이었다.

```
'클렌징' + '클렌징밤'      대분류와 소분류가 함께 잡힘
'토너' + '토너패드'        같은 제품
'미스트' + '바디미스트'    같은 제품
'크림' + '토너'            상품명에 카테고리 나열 ("크림/스킨케어/크림")
'픽서' + '마스카라'        "픽스 디테일 헤어 마스카라" 한 제품
```

→ 대신 **`+` 를 경계로 나눠 양쪽 제형을 각각** 본다. 24건으로 좁혀지고
대부분 진짜 세트였다. 1+1 은 `+` 뒤에 제형어가 없어 자동으로 걸러진다.

### 2-1-B. 대립 제형쌍은 하나씩 실측하고 추가한다

`CONFLICTING_FORMS` 는 한 번에 만들지 않고, C·D등급 검사에서 실제 오매칭이
나올 때마다 그 조합의 영향 건수를 재보고 추가해 왔다.

| 추가한 쌍 | 실측 영향 | 판단 |
|---|---:|---|
| 아이/핸드/바디크림 vs 크림 | — | v7.12.0 |
| 선세럼 vs 선크림 | — | v7.12.0 |
| 샴푸 vs 컨디셔너/트리트먼트 | — | v7.12.0 |
| 미스트 vs 픽서 | — | v7.12.0 |
| 클렌징폼 vs 클렌징오일/밤 | — | v7.12.0 |
| **세럼 vs 크림** | **4건, 전부 실제 오매칭** | v7.23.0 |

'세럼 vs 크림'은 흔한 조합이라 영향이 클까 걱정했는데 실측 4건뿐이었다.
같은 라인에서 제형만 다른 제품이 많아 이름 유사도로는 안 걸린다
(리쥬란 더마힐러 앰플 -> 크림, 아누아 어성초77 세럼 -> 수딩 크림).

→ **흔해 보이는 조합이라도 재보기 전엔 모른다.** 재보고 넣는다.

### 2-2. 제형 집합이 겹치지만 완전히 같지 않으면 제외 (v7.12.0에서 기각)

제외목록의 76건을 잡는 대신 **정상 매칭 625건**이 함께 걸렸다.
→ 대신 실측으로 확인한 대립쌍 9개만 쓴다(`CONFLICTING_FORMS`).

### 2-3. 성분어가 하나도 안 겹치면 다른 제품 (v7.16.0에서 보완)

이 조건으로는 실제 사례를 놓친다.

```
아누아 'PDRN 히알루론산 미스트'  vs  'PDRN 콜라겐 글로우 세럼 미스트'
-> PDRN 이 겹쳐서 통과. 그러나 히알루론산과 콜라겐은 다른 라인이다.
```

→ **양쪽이 서로에게 없는 성분을 각각 내세울 때**로 바꿨다.
한쪽만 성분을 밝힌 경우는 걸리지 않아야 한다(생략이 흔하다).

### 2-4. 브랜드 대응을 1건만 보고 사전에 등록 (v5.7.1에서 확인)

'판단불가 + 이름 유사도 0.8+' 구간 57종을 눈으로 확인하니 **정확도가
정확히 절반**이었다(28종만 맞음). 1건짜리를 그대로 받으면 사전 절반이
오염된다. 사전 오염은 이후 모든 판정에 퍼지고 추적이 어렵다.

→ 자동 규칙은 **2건 이상 같은 대응**일 때만 받는다. 그 아래는 육안으로만.

### 2-5. 유사도 임계를 낮춰 오매칭 제거 (v4.5.0 이후 반복 확인)

임계를 조금만 낮추면 정상 매칭이 함께 사라진다.

```
花王 '큐레루 딥 모이스처 스프레이'  =  나수 'Curel_딥 모이스처 스프레이'
原料工房 '티트리 그린 바디워시'      =  원료공방 '그린더마 티트리 바디워시'
```

→ 두 척도(단어겹침·글자조각)를 **모두** 통과해야 제외한다. 한 척도만 쓰면
띄어쓰기('맑은쌀 꿀채운'/'맑은쌀꿀채운')나 표기('UV'/'유브이') 차이로 오탐.

### 2-6. 스마트스토어 상품명을 서버에서 수집 (v7.9.0에서 중단)

네 가지를 전부 시험해도 뚫리지 않았다.

```
GitHub Actions            HTTP 429   데이터센터 IP 대역 차단
국내 가정용 PC + 스크립트   HTTP 490   자동화 접근 거부
국내 가정용 PC + 브라우저   로그인 페이지로 이동
로그인한 브라우저          HTTP 429
```

→ 실제 크롬(`channel="chrome"`)을 띄우면 통과한다. 수집은 **로컬 PC에서**
하고 결과만 `data/collected_pages.json` 으로 넣는다(2,465건 확보).

번들 크로미움으로 시험해놓고 "로그인해도 안 된다"고 결론냈던 게 오판이었다.
조건을 두 개 동시에 바꾸면 무엇이 원인인지 알 수 없다.

---

## 3. 판정 순서와 그 이유

`confidence_tier()` 의 판정 순서는 임의가 아니다.

```
1. 성분 불일치 (브랜드 확인된 경우)   -> D
2. 세트                              -> S
3. 브랜드 또는 제형 확인 실패          -> D
4. 색상 불일치                        -> D
5. 이름이 정확히 같지 않음             -> C
6. 용량 불일치                        -> B
7. 나머지                             -> A
```

- **성분을 세트보다 먼저** 보는 이유: 세트 구성이 달라도 성분이 아예
  다르면 오매칭이다.
- **이름이 애매하면 용량까지 확인하고 등급을 정한다**(v7.24.0). 예전엔
  이름에서 걸리면 바로 C로 보내고 용량을 아예 안 봐서, '같은 제품인데
  용량만 다른' 건 41건이 C에 숨어 있었다.
      라보에이치 두피강화 탈모샴푸 700ml -> 400ml (이름 유사도 0.33)
      스킨1004 마다가스카르 센텔라 앰플 55ml -> 100ml
      닥터지 레드블레미쉬 수딩 토너 300ml -> 500ml
  C 는 '이름이 왜 다른지'를 볼 구간이고 B 는 '용량만' 확인할 구간이다.
  어느 쪽을 봐야 하는지가 등급으로 갈려야 한다.

### 3-1. 수량·매수는 총량으로 환산해서 비교한다 (v7.25.0)

confidence_tier 주석의 설계("B = 용량/**수량**만 다름")에 수량이 처음부터
있었는데, 구현은 용량(ml/g)만 보고 있었다. extract_sheet_count()는 함수만
있고 등급 판정에 연결돼 있지 않았다. 실측: A등급 273건 중 **30건**이
'1+1 vs 단품', '10매 vs 1매(23g)' 같은 수량 불일치였다. 뉴클리드
마스크팩은 10매 가격(2,500円)과 1매 가격(2,000원)이 A로 나란히 있었다.

단순 비교로는 안 된다 — 매수와 묶음개수가 양쪽에 **교차 표기**된다.

```
"다이브인 마스크 10개"   =  "마스크 10매, 1개"      같은 상품
"페이스 필름 (3+1)"      =  "페이스필름 4매"         같은 상품
"60장x2개"               =  "[1+1] 60매"            같은 상품 (120매)
"9매입 (9+1)"            =  10매   (N+M이 매수 합산)
"[1+1] 60매"             =  120매  (N+M이 묶음 배수)
```

N+M은 숫자가 매수와 이어지면 합산, 안 이어지면 배수다 — 뜻이 문맥에 따라
정반대라 이 구분 없이는 어느 한쪽이 반드시 오탐난다. total_count()로 총량
환산 후 비교하며, 실측 오탐 4건('6장입', '10EA', 교차 표기 2건)을 정규식·
환산 규칙에 반영했다(회귀검사 t77이 전부 고정).

- 무표기는 단품(1)으로 본다. 성분(2-3)과 달리 수량은 표기가 없으면 실제로
  1개다(상거래 관행).
- 세트는 보지 않는다(S등급에서 처리).
- 이름 일치 구간(A 후보)만 적용한다 — 실측한 범위가 여기까지다. C 구간
  확대는 영향을 측정한 뒤에 한다.
- 제외가 아니라 강등만 한다(1-1 원칙).

또 하나: '매\b' 정규식은 "10매입"(뒤가 단어문자)과 "70장"을 아예 못
읽었다. 미스드래곤 매수 오매칭이 그동안 안 잡힌 실제 원인이 이것이었다.

### 3-2. '본품 + α'는 α가 무엇인지 갈라서 본다 (v7.26.0)

큐텐은 본품 단품인데 한국 판매처는 기획 구성인 건이 42건 있었다. 전부
세트로 묶으면 멀쩡한 매칭이 S로 사라지고, 전부 무시하면 실제로 두 배를
사는 건이 A로 남는다. α를 세 갈래로 나눈다.

```
증정(gift)     파우치·미니 15ml·샘플   가격 영향 거의 없음  -> 무시
본품추가(main) 동량 리필·1+1           실제 수량이 늘어남   -> 총량 합산
다른제품(other) 토너 + 크림             비교가 성립 안 함    -> 세트(S)
```

**판정 순서가 결정적이다.** 용량 비율을 제형보다 먼저 보면 '토너 350ml +
크림 20ml'(다른 제품)이 소량이라는 이유로 증정이 된다. 순서를 제형 →
용량으로 뒤집자 다른제품 검출이 13건 → 39건으로 늘고 오분류가 사라졌다.
'리필'은 제형어가 뒤에 붙어 있어도 본품이다('앰플 100ml + 리필 100ml
기획상품 탈모 두피에센스'가 뒤의 '에센스' 때문에 다른 제품으로 읽혔다).

용량 비율 기준은 0.5다. 실측상 절반 미만은 '휴대용 미니 15ml'·'기획
(+4ml)' 처럼 맛보기였고, 절반 이상은 '250ml+리필250ml'·'60매+리필60매'
처럼 실제 두 배였다.

**비대칭이 핵심이다.** 증정을 무시하는 건 **한국측(사는 쪽)만**이다.
큐텐측은 내가 파는 구성이라 '크림 50ml + 미니 15ml' 로 올려놨으면 미니도
사서 보내야 한다. 양쪽에서 지우면 못 맞추는 구성이 A로 남는다. 처음에
양쪽 대칭으로 짰다가 이 이유로 되돌렸다.

또 하나: 세트 판정에 검증 승자의 `name` 이 들어가고 있었다. 로컬
수집(v7.10.0)으로 갱신된 정확한 상품명에 '더블기획'·'기획(+10ml)' 이
적혀 있어도 판정이 그걸 못 봤다 — 실측 38건이 이 이유로 세트를 놓쳤다.
검수 화면에 보이는 이름(`kr_name_display`)으로 판정한다.

**남은 것:** 큐텐측에만 부가 구성이 있는 경우('패드 70매 + 리필 70매' →
한국 '70매')는 아직 안 잡힌다. total_count 가 첫 조각만 세기 때문이다.
별도 규칙이 필요하고 영향 측정이 아직이다.

---

## 4. 제형 판정의 함정

### 4-1. 세부 제형이 상위어를 문자열로 포함한다

`'아이크림'` 안에 `'크림'` 이 들어있어 둘 다 잡힌다. 그대로 교집합을 보면
`아이크림`과 `크림`이 같은 것이 된다.
→ `_narrow_forms()` 로 세부 쪽이 있으면 상위어를 지운다.

### 4-2. 복합 표기는 뒤에 오는 말이 정체다

`'크림 마스크'` 는 크림이 아니라 마스크다. 앞의 말은 내용물 설명이다.
→ `COMPOUND_FORMS` 로 앞쪽 제형을 지운다.
실측: 셀리맥스 '브라이트닝 크림 35ml' 이 '브라이트닝 크림 마스크 4매'와
A등급으로 묶여 있었다.

### 4-3. 대분류/소분류는 포함관계로 봐야 한다

`'클렌징'` 과 `'클렌징 폼'` 은 같고, `'클렌징 폼'` 과 `'클렌징 오일'` 은
다르다. 대분류 이름이 소분류 문자열에 들어있어 단순 교집합으로는
구분되지 않는다.
→ 비교는 소분류로만 하고 대분류는 포함관계 판정에만 쓴다(`FORM_PARENTS`).

### 4-4. 자외선 차단 등급의 '+'는 등급 기호다

`'SPF50+ PA++++ 50ml'` 에서 마지막 `+` 와 용량이 붙어 `'+ 50ml'` 로 읽혀
멀쩡한 선크림이 세트로 잡혔다. 여기 쓰인 `+` 는 등급을 나타내는 기호이지
`'제품 A + 제품 B'` 의 `+` 가 아니다.

- PA 등급은 `PA+` ~ `PA++++` 네 단계
- SPF 는 숫자 뒤에 `+` 가 붙을 수 있다(`SPF50+` = 'SPF 50 이상')
- 구분자가 제각각이다. 실측 표기 형태:

```
SPF50+ PA++++     SPF 50+ PA++++    SPF50+/PA++++
SPF50+PA++++      SPF50+, PA++++    SPF50+ / PA++++
SPF38 PA++        SPF20 PA++        SPF50+
```

→ 세트 판정 전에 `SPF_NOTATION_RE` 로 먼저 지운다. 등급 표기가 있으면서
**동시에 진짜 세트인 경우**(`선크림 SPF50+ PA++++ 50ml + 선스틱 20g`)는
그대로 세트로 잡혀야 한다 — 회귀검사가 두 경우를 다 확인한다.

---

## 5. 브랜드 판정의 함정

### 5-1. 네이버 brand 칸은 판매처명인 경우가 많다

```
viviscal -> brand '슈퍼대디',   상품명 '비비스칼 프로 viviscal pro'
呂       -> brand '아모레퍼시픽', 상품명 '려 루트젠 두피에센스'
```

→ 상품명에서도 찾는다. 사전에 없어도 5자 이상이면 그대로 찾고, 4자는
상품명 앞부분일 때만(상품명은 대개 브랜드로 시작한다).
짧은 영문은 다른 단어에 우연히 들어간다('LOA'가 'FLOAT'에).

### 5-2. 영문 원본을 한글 판매처명과 직접 비교할 수 없다

`LE LABO` vs `르라보` 는 알파벳만 남기면 절대 안 맞는다. 가나는 판단불가로
빠지는데 영문만 빠져 있어 불일치 813건 중 245건(30%)이 이 유형이었다.
→ 판매처명에 라틴문자가 없으면 `unknown`.

### 5-3. 로컬 수집 브랜드가 가장 정확하다

`data/collected_pages.json` 의 brand 는 상품 페이지에서 직접 읽은 값이다.
이걸로 대조해 오매칭 187건을 찾았다(나이키 운동화·후지필름 카메라·
프라모델까지 검수 목록에 있었다).

---

## 6. 제외 기준의 변천

### 6-1. '원산지'가 아니라 '국내 유통 여부'

처음엔 일본·유럽 브랜드를 통째로 뺐는데 기준이 잘못됐다. 이 사업은
한국에서 사서 큐텐재팬에 파는 구조라, 해외 브랜드라도 국내 유통이 있으면
대상이다. 비오더마·라로슈포제·큐렐·케라스타즈는 국내 약국·올리브영에서
정상 유통된다.
→ `foreign_brands.json` 에는 **국내 유통이 없는** 34종만 남긴다.

### 6-2. 가타카나라고 해외 브랜드가 아니다

`アヌア`=아누아, `メディキューブ`=메디큐브, `雪花秀`=설화수, `呂`=려는
전부 한국 브랜드다. 문자 종류로 자르면 한국 브랜드 수백 건이 사라진다.
→ 목록으로 관리하고, 회귀검사가 이 대표 브랜드들이 섞이지 않았는지 본다.

### 6-3. 목록을 공백으로 쪼개면 다단어 브랜드가 깨진다

`'LE LABO'` 가 `'LE'` + `'LABO'` 로, `'Suntory Wellness'` 가
`'Suntory'` + `'Wellness'` 로 갈라져 있었다. 조각은 어떤 브랜드와도
일치하지 않아 **조용히 아무 일도 안 한다** — 걸러진 줄 알았던 상품이
그대로 들어왔다. C구간을 눈으로 보지 않았으면 못 찾았다.

---

## 7. 새 규칙을 만들 때 점검할 것

1. 같은 시도가 이 문서 2절에 이미 있는가
2. 영향 건수를 실측했는가. 오탐 비율은 얼마인가
3. 정상 매칭이 걸리는 대표 사례를 찾아봤는가
4. 판정 순서상 앞 단계에서 이미 걸러지지는 않는가(3절)
5. `simulation.py` 에 검사 항목을 추가했는가
6. 되돌릴 수 있게 원본 값을 남기는가
