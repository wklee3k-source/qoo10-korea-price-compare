# 판정 로직 설계 이력

새 판정 규칙을 만들기 전에 이 문서를 먼저 읽는다. 여기 적힌 것들은 전부
**실제로 시도했다가 실패했거나, 실패를 겪은 뒤에 고쳐 만든 것**이다.
같은 함정을 다시 밟지 않으려고 남긴다.

커밋 메시지에도 근거를 적어두지만, 커밋은 시간순이라 "왜 이 조건이
이렇게 좁은가"를 찾으려면 수십 개를 뒤져야 한다. 이 문서는 그 이유만
모아둔 것이다.

---

## 1. 반복해서 확인된 원칙

### 1-1. 자동으로 버리지 않는다

기계가 걸러낸 것 중 상당수가 정상 매칭이다. 한번 버리면 **버려졌다는
사실조차 보이지 않는다.** 그래서 규칙은 등급을 내리거나 표시를 붙이는
쪽으로 쓰고, 목록에서 제거하는 건 사람이 눈으로 확인한 것만 한다.

- 자동 규칙 → `build_review.py` 코드
- 사람 판단 → `data/manual_exclusions.json` (452건)

두 경로를 섞지 않는다. 사람 판단을 코드로 일반화하려다 정상 매칭이
대량으로 사라진 사례가 아래에 여러 건 있다.

### 1-2. 규칙을 넓히기 전에 반드시 영향 건수를 잰다

"이 조건이면 잡히겠다" 싶어도 실제로 재보면 오탐이 훨씬 많은 경우가
대부분이었다. 재보지 않고 넣었다가 되돌린 적이 여러 번 있다.

### 1-3. 판단 근거가 사람이면 데이터로 남긴다

한 번 확인한 것을 다음 회차에 또 확인하지 않으려면 파일로 남아야 한다.
`brand_translations_learned.json`, `foreign_brands.json`,
`manual_exclusions.json`, `collected_pages.json` 이 그 역할이다.

---

## 2. 시도했다가 못 쓴 방법들

### 2-1. 제형이 두 개 이상이면 세트 (v7.20.0에서 기각)

실측 255건이 걸렸는데 그중 **A등급 86건이 멀쩡한 단품**이었다.

```
'클렌징' + '클렌징밤'      대분류와 소분류가 함께 잡힘
'토너' + '토너패드'        같은 제품
'미스트' + '바디미스트'    같은 제품
'크림' + '토너'            상품명에 카테고리 나열 ("크림/스킨케어/크림")
'픽서' + '마스카라'        "픽스 디테일 헤어 마스카라" 한 제품
```

→ 대신 **`+` 를 경계로 나눠 양쪽 제형을 각각** 본다. 24건으로 좁혀지고
대부분 진짜 세트였다. 1+1 은 `+` 뒤에 제형어가 없어 자동으로 걸러진다.

### 2-1-B. 대립 제형쌍은 하나씩 실측하고 추가한다

`CONFLICTING_FORMS` 는 한 번에 만들지 않고, C·D등급 검사에서 실제 오매칭이
나올 때마다 그 조합의 영향 건수를 재보고 추가해 왔다.

| 추가한 쌍 | 실측 영향 | 판단 |
|---|---:|---|
| 아이/핸드/바디크림 vs 크림 | — | v7.12.0 |
| 선세럼 vs 선크림 | — | v7.12.0 |
| 샴푸 vs 컨디셔너/트리트먼트 | — | v7.12.0 |
| 미스트 vs 픽서 | — | v7.12.0 |
| 클렌징폼 vs 클렌징오일/밤 | — | v7.12.0 |
| **세럼 vs 크림** | **4건, 전부 실제 오매칭** | v7.23.0 |

'세럼 vs 크림'은 흔한 조합이라 영향이 클까 걱정했는데 실측 4건뿐이었다.
같은 라인에서 제형만 다른 제품이 많아 이름 유사도로는 안 걸린다
(리쥬란 더마힐러 앰플 -> 크림, 아누아 어성초77 세럼 -> 수딩 크림).

→ **흔해 보이는 조합이라도 재보기 전엔 모른다.** 재보고 넣는다.

### 2-2. 제형 집합이 겹치지만 완전히 같지 않으면 제외 (v7.12.0에서 기각)

제외목록의 76건을 잡는 대신 **정상 매칭 625건**이 함께 걸렸다.
→ 대신 실측으로 확인한 대립쌍 9개만 쓴다(`CONFLICTING_FORMS`).

### 2-3. 성분어가 하나도 안 겹치면 다른 제품 (v7.16.0에서 보완)

이 조건으로는 실제 사례를 놓친다.

```
아누아 'PDRN 히알루론산 미스트'  vs  'PDRN 콜라겐 글로우 세럼 미스트'
-> PDRN 이 겹쳐서 통과. 그러나 히알루론산과 콜라겐은 다른 라인이다.
```

→ **양쪽이 서로에게 없는 성분을 각각 내세울 때**로 바꿨다.
한쪽만 성분을 밝힌 경우는 걸리지 않아야 한다(생략이 흔하다).

### 2-4. 브랜드 대응을 1건만 보고 사전에 등록 (v5.7.1에서 확인)

'판단불가 + 이름 유사도 0.8+' 구간 57종을 눈으로 확인하니 **정확도가
정확히 절반**이었다(28종만 맞음). 1건짜리를 그대로 받으면 사전 절반이
오염된다. 사전 오염은 이후 모든 판정에 퍼지고 추적이 어렵다.

→ 자동 규칙은 **2건 이상 같은 대응**일 때만 받는다. 그 아래는 육안으로만.

### 2-5. 유사도 임계를 낮춰 오매칭 제거 (v4.5.0 이후 반복 확인)

임계를 조금만 낮추면 정상 매칭이 함께 사라진다.

```
花王 '큐레루 딥 모이스처 스프레이'  =  나수 'Curel_딥 모이스처 스프레이'
原料工房 '티트리 그린 바디워시'      =  원료공방 '그린더마 티트리 바디워시'
```

→ 두 척도(단어겹침·글자조각)를 **모두** 통과해야 제외한다. 한 척도만 쓰면
띄어쓰기('맑은쌀 꿀채운'/'맑은쌀꿀채운')나 표기('UV'/'유브이') 차이로 오탐.

### 2-6. 스마트스토어 상품명을 서버에서 수집 (v7.9.0에서 중단)

네 가지를 전부 시험해도 뚫리지 않았다.

```
GitHub Actions            HTTP 429   데이터센터 IP 대역 차단
국내 가정용 PC + 스크립트   HTTP 490   자동화 접근 거부
국내 가정용 PC + 브라우저   로그인 페이지로 이동
로그인한 브라우저          HTTP 429
```

→ 실제 크롬(`channel="chrome"`)을 띄우면 통과한다. 수집은 **로컬 PC에서**
하고 결과만 `data/collected_pages.json` 으로 넣는다(2,465건 확보).

번들 크로미움으로 시험해놓고 "로그인해도 안 된다"고 결론냈던 게 오판이었다.
조건을 두 개 동시에 바꾸면 무엇이 원인인지 알 수 없다.

---

## 3. 판정 순서와 그 이유

`confidence_tier()` 의 판정 순서는 임의가 아니다.

```
1. 성분 불일치 (브랜드 확인된 경우)   -> D
2. 세트                              -> S
3. 브랜드 또는 제형 확인 실패          -> D
4. 색상 불일치                        -> D
5. 이름이 정확히 같지 않음             -> C
6. 용량 불일치                        -> B
7. 나머지                             -> A
```

- **성분을 세트보다 먼저** 보는 이유: 세트 구성이 달라도 성분이 아예
  다르면 오매칭이다.
- **이름이 애매하면 용량까지 확인하고 등급을 정한다**(v7.24.0). 예전엔
  이름에서 걸리면 바로 C로 보내고 용량을 아예 안 봐서, '같은 제품인데
  용량만 다른' 건 41건이 C에 숨어 있었다.
      라보에이치 두피강화 탈모샴푸 700ml -> 400ml (이름 유사도 0.33)
      스킨1004 마다가스카르 센텔라 앰플 55ml -> 100ml
      닥터지 레드블레미쉬 수딩 토너 300ml -> 500ml
  C 는 '이름이 왜 다른지'를 볼 구간이고 B 는 '용량만' 확인할 구간이다.
  어느 쪽을 봐야 하는지가 등급으로 갈려야 한다.

### 3-1. 수량·매수는 총량으로 환산해서 비교한다 (v7.25.0)

confidence_tier 주석의 설계("B = 용량/**수량**만 다름")에 수량이 처음부터
있었는데, 구현은 용량(ml/g)만 보고 있었다. extract_sheet_count()는 함수만
있고 등급 판정에 연결돼 있지 않았다. 실측: A등급 273건 중 **30건**이
'1+1 vs 단품', '10매 vs 1매(23g)' 같은 수량 불일치였다. 뉴클리드
마스크팩은 10매 가격(2,500円)과 1매 가격(2,000원)이 A로 나란히 있었다.

단순 비교로는 안 된다 — 매수와 묶음개수가 양쪽에 **교차 표기**된다.

```
"다이브인 마스크 10개"   =  "마스크 10매, 1개"      같은 상품
"페이스 필름 (3+1)"      =  "페이스필름 4매"         같은 상품
"60장x2개"               =  "[1+1] 60매"            같은 상품 (120매)
"9매입 (9+1)"            =  10매   (N+M이 매수 합산)
"[1+1] 60매"             =  120매  (N+M이 묶음 배수)
```

N+M은 숫자가 매수와 이어지면 합산, 안 이어지면 배수다 — 뜻이 문맥에 따라
정반대라 이 구분 없이는 어느 한쪽이 반드시 오탐난다. total_count()로 총량
환산 후 비교하며, 실측 오탐 4건('6장입', '10EA', 교차 표기 2건)을 정규식·
환산 규칙에 반영했다(회귀검사 t77이 전부 고정).

- 무표기는 단품(1)으로 본다. 성분(2-3)과 달리 수량은 표기가 없으면 실제로
  1개다(상거래 관행).
- 세트는 보지 않는다(S등급에서 처리).
- 이름 일치 구간(A 후보)만 적용한다 — 실측한 범위가 여기까지다. C 구간
  확대는 영향을 측정한 뒤에 한다.
- 제외가 아니라 강등만 한다(1-1 원칙).

또 하나: '매\b' 정규식은 "10매입"(뒤가 단어문자)과 "70장"을 아예 못
읽었다. 미스드래곤 매수 오매칭이 그동안 안 잡힌 실제 원인이 이것이었다.

### 3-2. '본품 + α'는 α가 무엇인지 갈라서 본다 (v7.26.0)

큐텐은 본품 단품인데 한국 판매처는 기획 구성인 건이 42건 있었다. 전부
세트로 묶으면 멀쩡한 매칭이 S로 사라지고, 전부 무시하면 실제로 두 배를
사는 건이 A로 남는다. α를 세 갈래로 나눈다.

```
증정(gift)     파우치·미니 15ml·샘플   가격 영향 거의 없음  -> 무시
본품추가(main) 동량 리필·1+1           실제 수량이 늘어남   -> 총량 합산
다른제품(other) 토너 + 크림             비교가 성립 안 함    -> 세트(S)
```

**판정 순서가 결정적이다.** 용량 비율을 제형보다 먼저 보면 '토너 350ml +
크림 20ml'(다른 제품)이 소량이라는 이유로 증정이 된다. 순서를 제형 →
용량으로 뒤집자 다른제품 검출이 13건 → 39건으로 늘고 오분류가 사라졌다.
'리필'은 제형어가 뒤에 붙어 있어도 본품이다('앰플 100ml + 리필 100ml
기획상품 탈모 두피에센스'가 뒤의 '에센스' 때문에 다른 제품으로 읽혔다).

용량 비율 기준은 0.5다. 실측상 절반 미만은 '휴대용 미니 15ml'·'기획
(+4ml)' 처럼 맛보기였고, 절반 이상은 '250ml+리필250ml'·'60매+리필60매'
처럼 실제 두 배였다.

**비대칭이 핵심이다.** 증정을 무시하는 건 **한국측(사는 쪽)만**이다.
큐텐측은 내가 파는 구성이라 '크림 50ml + 미니 15ml' 로 올려놨으면 미니도
사서 보내야 한다. 양쪽에서 지우면 못 맞추는 구성이 A로 남는다. 처음에
양쪽 대칭으로 짰다가 이 이유로 되돌렸다.

또 하나: 세트 판정에 검증 승자의 `name` 이 들어가고 있었다. 로컬
수집(v7.10.0)으로 갱신된 정확한 상품명에 '더블기획'·'기획(+10ml)' 이
적혀 있어도 판정이 그걸 못 봤다 — 실측 38건이 이 이유로 세트를 놓쳤다.
검수 화면에 보이는 이름(`kr_name_display`)으로 판정한다.

**남은 것:** 큐텐측에만 부가 구성이 있는 경우('패드 70매 + 리필 70매' →
한국 '70매')는 아직 안 잡힌다. total_count 가 첫 조각만 세기 때문이다.
별도 규칙이 필요하고 영향 측정이 아직이다.

### 3-3. 큐텐 증정이 더 많으면 그 초과분을 제목에서 지운다 (v7.27.0)

큐텐에 '크림 50ml + 15ml + 15ml' 로 올려놨는데 한국에서는 50ml 단품밖에
못 사면 15ml 두 개를 줄 수가 없다. 못 지킬 구성을 파는 셈이라 초과분을
업로드용 제목에서 뺀다. 실측 14건.

지우는 건 파는 물건을 바꾸는 일이라 기준을 3-2 보다 좁게 잡는다.

```
조각에 제형어가 있거나 본품 제형을 모르면 안 지운다
   실측 오분류: '부스터 120ml + 세럼 45ml'(2종 구성)이 '부스터'가 제형
   사전에 없어 비교가 안 되는 바람에 증정으로 읽혔다
조각이 12자를 넘으면 안 지운다
   실측 오분류: '큐리베어SOS 멜더 시스템 (1.5ml x 20本)'
번역본과 원문의 '+' 조각 수가 다르면 손대지 않는다
   대응시킬 수 없다
```

이 세 조건으로 31건 → 14건이 된다.

**적용 위치가 자동수정 체인의 맨 끝이어야 한다.** 앞의 용량·수량
자동수정이 `q["title"]` 원문을 기준으로 다시 쓰기 때문에, 먼저 지우면
그 결과가 덮여 사라진다 — 실제로 '150ml+30ml' 이 삭제 대신 '330ml+30ml'
로만 바뀌어 나왔다.

문자열 처리에도 함정이 둘 있다.
- 조각 끝의 닫는 괄호까지 지우면 짝이 깨진다('(+7ml)' → '(',
  '【120ml+15ml】' → '【120ml'). 닫는 괄호는 남긴다.
- 뒤에서부터 찾아 지운다. 앞에서 찾으면 '(20ml+20ml)' 에서 첫 조각이
  지워져 '(+20ml)' 가 된다.

지운 내역은 `change_notes` 와 취소선으로 반드시 남긴다(1-1: 조용히
사라지면 잘못 지워도 알 수 없다).

### 3-4. 제형이 같아도 별개 제품일 수 있다 (v7.28.0)

사장님 지적: `부스터 120ml + 세럼 45ml` 은 증정이 아니라 2종 세트다.
그런데 '부스터'가 제형 사전에서 세럼으로 매핑돼 본품과 **같은 제형**이
되는 바람에, 3-2 의 제형 비교를 통과하고 용량 비율(0.375)만 보고 증정이
됐다. 제형이 다를 때만 세트로 보는 건 부족하다.

조각이 **제형어와 자체 용량을 모두** 가지고 **본품에도 용량**이 있으면
두 제품이 나란히 있는 것으로 본다. 셋 중 하나라도 빠지면 성분·설명
나열일 뿐이다.

```
부스터 120ml + 세럼 45ml        제형·용량·본품용량 셋 다 있음  -> 세트
크림 50ml + 휴대용 미니 15ml    조각에 제형어 없음             -> 증정
레티놀 0.1 + 카페인 아이크림    조각에 용량 없음               -> 설명
유산균 + 시카 세럼 50ml         본품에 용량 없음               -> 설명
```

**먼저 지워야 하는 표기가 둘 있다.** 수량('1+1', '9+1')과 성분 함량
나열('AHA 30% + BHA 2%')은 구성이 아니라 표기다. 안 지우고 '+' 로 나누면
뒤 조각이 상품명 전체가 되고('1] 아누아 선크림 50ml'), 거기 든 제형어
때문에 별개 제품으로 읽힌다 — **실측 83건 오탐**. 이걸 안 잡고 '제형어가
있으면 세트' 규칙만 넣었다면 멀쩡한 1+1 상품이 대량으로 S로 사라졌다.

조건을 좁혀가며 잰 수치: 제형어만 → 98건 변동(오탐 다수) → 수량·성분
마스킹 → 49건 → 양쪽 용량 조건 → 28건(오탐 거의 없음).

### 3-5. 용량 추출은 대소문자를 구분하면 안 된다 (v7.29.0)

큐텐 원문이 '30ML'처럼 대문자면 `extract_volume_ml()` 이 아예 못 읽고
`None`(판단불가)이 됐다. 판단불가는 등급 판정에서 빠지므로, 용량이 실제로
다른 상품이 이름 유사도만으로 A(완전일치)에 남았다.

```
큐텐   아나수이 오드 트왈렛 30ML EDT SP
매칭   안나수이 판타지아 오드 트왈렛 EDT SP 75ML
```

30ml과 75ml — 가격도 24배 차이(3,080円/73,100원)로 이미 신호가 있었다.
바로 아래 등급(B)에 같은 상품의 다른 매칭이 있었는데, 거긴 소문자 'ml'
이라 정상적으로 용량불일치가 잡혀 있었다. 같은 종류 오류가 등급 사이에
갈라져 있으면 발견하기 어렵다 — 검수 화면 한 페이지만 봐서는 안 보이고,
같은 상품을 여러 등급에서 교차 확인해야 드러난다.

대책은 `re.IGNORECASE` 플래그 추가뿐이다. 'mg'(함량 표기)는 이 패턴이
원래도 안 잡는다 — 'g' 앞에 'm'이 있어 리터럴 매칭이 안 되기 때문에,
대소문자를 무시해도 새로 잘못 잡히지 않는다. 실측 11건 전부 새로 찾는
방향이었고 오탐은 0건이었다.

### 3-6. 숫자 없는 배수 표기도 수량으로 본다 (v7.30.0)

'더블팩'·'듀오팩'·'트윈팩'은 숫자 없이 2개들이를 뜻한다. 수량 추출이
숫자('1+1', '2개', '2PACK')만 보고 있어 이 표기를 아예 못 읽었다.

```
큐텐   하이알루론 에피셀린세럼 30ml
매칭   유세린 하이알루론 에피셀린 세럼 30ml 더블팩         (가격 21배 차이)
```

두 건 모두 A(완전일치)에 남아 있었다. 가격 비율을 등급별로 훑어보다가
찾았다 — 중앙값(8.3배)에서 크게 벗어난 A등급 건을 역으로 짚어보는
방식으로, 수량·용량 정규식만으로는 안 잡히는 표기 누락을 찾을 수 있다.

'더블/듀오/트윈 + 팩/기획/구성' 패턴을 추가했다. '1+1 ... 230ml+230ml
더블기획'처럼 이미 용량 대응(3-2)으로 정상 처리되던 건과 중복 계산되지
않는지 확인 후 반영 — 재빌드 결과 의도한 2건만 이동했다.

**참고**: 브랜드칸 불일치처럼 보이는 것 중엔 진짜 버그가 아닌 경우도
있다. '오믹셀'(판매처 브랜드칸) vs 'Lebel'(사전) 은 상품명 자체에
'르벨'이 있어 5-1 로직(판매처명 대신 상품명에서 브랜드 확인)이 정상
작동한 것이었다. 가격 이상치라고 전부 코드 문제는 아니다.

### 3-7. 수량 불일치를 C 구간까지 확장 (v7.31.0)

v7.25.0에서 수량 불일치 규칙을 넣을 때 이름이 일치하는 구간(A 후보)에만
적용하고, C 구간(이름 불일치)까지 넓히는 건 영향을 측정한 뒤로 미뤄뒀다.

실측: C등급 315건 중 62건이 실제로는 수량 표기 차이였다.

```
큐텐   아쿠아 스쿠알란 모이스처 크림, 60ml, 2개
매칭   아쿠아 스쿠알란 수분크림                     (60ml 단품)
```

'이름이 왜 다른가'를 볼 필요가 없는데도 C에 섞여 있었다. `if not
name_exact: return "B" if vol_mismatch else "C"` 에 `count_mismatch`를
더해 이 62건을 B로 옮겼다 — D가 아니라 B인 이유는, 성격이 '이름 다름'이
아니라 '수량만 확인하면 됨'으로 바뀌기 때문이다.

### 3-8. 증정 구간의 숫자를 본품 용량으로 잘못 집는다 (v7.32.0)

`extract_volume_ml(kr_name_display)` 이 상품명 전체에서 첫 숫자+단위를
찾다 보니, 본품보다 증정 구간의 숫자를 먼저 만나면 그걸 집는다.

```
마녀공장 글루타치온7 다크스팟 잡티세럼&미백패치 [+세럼 1.5ml 7일키트 증정]
```

본품은 30ml인데 증정의 '1.5ml'을 집어 검수 카드에 '용량 불일치'로 표시됐다.
등급 자체는 5ml 미만 판단보류 기준(MIN_TRUSTED_VOLUME_ML)에 우연히 걸려
안 틀렸지만, **화면에 보이는 배지가 틀려 있었다** — 사장님이 카드를 보고
판단하는 근거 자체가 잘못된 것이었다.

대책: `split_bonus()`로 본품 구간을 먼저 떼어 그 안에서 용량을 찾고,
없으면 검증 단계에서 받은 volume 필드, 그래도 없으면 전체 텍스트
순서로 본다. 본품 구간 자체에 용량이 적혀 있는 경우('350ml (+350ml
추가)')는 그대로 본품값을 쓴다 — 순서가 이렇게 3단이어야
'본품에 용량 없음'과 '본품에 용량 있음'을 다 커버한다.

**부수 발견(현재 영향 0건, 기록만)**: `+`가 상품명 맨 앞 괄호로 오면
('[+에센스8ml증정] 마녀공장...') split_bonus가 첫 조각을 빈 문자열로
보고 본품 전체를 증정으로 분류한다. 전 등급 스윕 결과 2건뿐이고 둘 다
다른 경로로 이미 정상 등급이라 지금은 손대지 않는다(1-2: 영향 없으면
규칙을 넓히지 않는다). 다음에 비슷한 표기가 늘면 재측정한다.

### 3-9. 브랜드 사전에 영문 별칭이 필요하다 (v7.33.0)

브랜드 사전(`brand_translations_learned.json`)은 한글 표기만 갖고 있는데,
로컬 수집(`collected_pages.json`)의 브랜드칸은 실제 페이지에서 읽은
영문·로마자 표기다("DR.G", "Medicube", "COSRX" 등). 한글끼리만
비교하면 둘이 만날 일이 없어 D(브랜드 불일치)로 떨어졌다.

실측: D등급 브랜드 불일치 72건 중 17종 26건이 이 유형이었다. 전부
`collected_pages.json` 실데이터에서 직접 관찰된 영문 표기만
`BRAND_ALIASES`에 등록했다(추측 등록 금지 — 2-4 원칙과 같은 이유,
1건 관찰로 사전을 오염시키면 절반은 틀린다). 18건이 D에서 다른 등급
(대부분 B·A)으로 정확히 이동했다 — 브랜드가 확인되니 다음 단계인
제형·이름·용량 판정으로 넘어간 것이다.

### 3-10. 브랜드 비교에서 공백 유무를 무시해야 한다 (v7.34.0)

로컬 수집 브랜드칸은 실제 페이지 표기를 그대로 가져오는데 '키시닝
뷰티'처럼 중간에 공백이 들어간다. 사전값 '키시닝뷰티'(공백 없음)와
글자는 완전히 같은데 단순 substring 비교로는 서로 남남이 됐다.

브랜드칸 비교와 상품명 폴백 비교 양쪽에 공백 제거 버전을 추가로
비교한다. 기존 정확 매칭은 그대로 두고 폴백으로만 추가했다 — 3건
(오지오토·바비브라운·키시닝뷰티)이 해소됐고, 진짜 다른 브랜드가
느슨해져서 잘못 붙는 오탐은 없었다(회귀검사로 고정).

### 3-11. playwright 버전을 고정해야 한다 (v7.35.0)

전체 재검증 도중 화해·무신사 서브프로세스가 매 호출마다 code=1로
죽어 검증이 멈췄다. 실제 오류는 "브라우저 없음, playwright install
하라"였는데, install-deps(시스템 패키지)만 돌고 install(브라우저
바이너리)은 캐시 히트 때문에 건너뛰고 있었다.

원인: `requirements.txt`가 `playwright>=1.50`으로 버전을 열어뒀다.
캐시 키는 이 파일의 해시인데, 버전 범위 표기 자체는 안 바뀌니 pip가
매번 최신(지금은 1.62.0)을 새로 받아도 캐시 키는 그대로다. 그 결과
캐시엔 예전에 넣어둔 옛날 버전용 브라우저만 남아 있고, 방금 새로 깐
playwright 1.62.0은 자기 버전에 맞는 브라우저 리비전을 못 찾는다.

`playwright==1.62.0`로 정확히 고정했다. 이러면 버전을 올릴 때마다
이 줄이 바뀌어야 하고, 그게 곧 requirements.txt 해시를 바꿔 캐시를
강제로 새로 받게 만든다 — 캐시 무효화를 "버전 고정"이라는 원래
해야 할 일에 자연히 편승시킨 것이다.

**같이 확인한 것**: 네이버쇼핑 API가 모든 요청에서 예외 없이 404였다.
URL·헤더 구성은 공식 문서와 일치해 코드 결함은 안 보였다 — 매 요청
동일하게 실패하는 패턴은 보통 네이버 개발자센터의 앱 등록 상태 문제일
때 나온다. 이 환경은 openapi.naver.com 접근이 막혀 있어 직접 재현은
못 했고, 사장님 확인이 필요하다.

### 3-12. 색조 제외는 카테고리만으로 안 된다 (v7.36.0)

C등급 전수검사에서 어반디케이 올나이터 세팅 스프레이가 **'매트' vs
'글로우'**(마감이 정반대인 별개 제품)로 매칭돼 있었다. 용량이 같아
오히려 잘 맞은 것처럼 보였다.

색조는 카테고리 코드(013·014·016)로 거르는데, 이 상품의 카테고리는
`120000012`(스킨케어)였다. 큐텐 셀러가 세팅 스프레이를 스킨케어
카테고리에 등록해두면 카테고리 그물을 그대로 빠져나온다.

**카테고리를 넓히는 걸로는 못 고친다.** 120000012 는 검수 대상 838건
대부분이 정상 스킨케어(크림·세럼·앰플)라 통째로 뺄 수 없다.

그래서 제형('픽서')으로 거른다. 단 헤어 픽서·스프레이는 헤어케어라
성격이 다르므로 남긴다 — 실측 16건이 메이크업 11 / 헤어 5 로 정확히
갈렸다.

함정 하나: 헤어 판별에 '컬'만 넣으면 '코드글로컬러' 같은 브랜드명에
걸려 메이크업 프라이머가 헤어로 새어나간다(실측 오탐 1건). '컬 고정'·
'컬링'으로 좁혀야 한다.

### 3-13. 소스가 죽은 상태에서 전체 재검증하면 데이터가 날아간다 (사고)

전체 재검증을 돌렸는데 4,455건이 **브랜드·상품명·가격·구매링크 전부
`null`** 로 돌아왔다. 재빌드하면 검수 대상이 1,138 → 3건이 된다.

재검증 시작 시점에 이미 Exa 크레딧이 소진돼 있었고, 네이버는 404,
화해·무신사는 playwright 오류로 죽어 있었다. **작동하는 소스가 하나도
없는 상태에서 전부 다시 조회**한 것이라, 잘 채워져 있던 기존 데이터가
전부 빈 결과로 덮였다.

재검증 직전 백업(`output/archive/hwahae_verified_before_full_reverify_2.json`)
으로 복원했다. 교훈:

- **재검증 전에 소스 상태를 먼저 확인한다.** 최소 1건을 시험 조회해서
  결과가 채워지는지 보고 시작한다.
- 재검증은 기존 값을 덮어쓰는 작업이라 백업이 필수다(이번엔 남겨둬서
  살았다).
- 검증 실패가 '기술적 실패'로 분류되면 기존 값을 유지하도록 하는 게
  근본 대책이다 — 아직 반영 안 됨.

### 3-14. 네이버쇼핑 API 영구 종료 (v7.37.0)

네이버가 검색 API를 NAVER API HUB로 이관하면서 **'쇼핑·책·전문자료'는
이관 대상에서 제외하고 2026년 7월 31일에 종료**했다. 유예 기간이 없고
기존 발급 키로도 호출이 불가하며, **대체 API도 제공되지 않는다.**
API HUB의 검색 API 목록에도 쇼핑은 없다(블로그·뉴스·백과·카페·지식iN·
웹문서·이미지·지역만).

즉 복구 방법이 없다. 앱 설정 문제가 아니라 서비스가 사라진 것이다.

**영향 측정(실측)**

```
승자가 네이버쇼핑이던 건    615건 (검수 대상 296건)
합의 2곳 충족 상실          342건 (검수 대상  49건)
합의 충족 총량            3,608 -> 3,266건
```

검수 대상 1,126건 중 296건(26%)이 직접 영향권이다. 화해(1,570건)가
최대 소스라 파이프라인이 멈추지는 않지만, 하루 100건을 채우려면
발굴·수확량을 약 1.35배 늘려야 한다.

`_search_naver()` 를 호출 없이 `None`(무결과) 반환으로 끊었다. 호출부
(재검색·수량재검색 등)는 그대로 뒀다 — 대체 소스를 붙일 때 그 자리에
넣으면 되기 때문이다. 종료 전 구현은 `_search_naver_disabled_2026()`
으로 남겨뒀다.

**살릴 수 있는 것**: 네이버 웹문서 검색(`naver_web_search.py`)은 이관
대상이라 API HUB로 옮기면 계속 쓸 수 있다(2027-06-30 까지는 기존 키도
동작). 이건 별도 작업으로 남아 있다.

### 3-15. 검증 실패가 기존 값을 지우면 안 된다 (v7.37.0)

3-13 사고의 근본 대책. '못 찾았다'는 결과는 '전에 찾았던 걸 지운다'는
뜻이 아닌데, 실패 경로들이 전부 빈 entry 를 그대로 기록하고 있었다.

실패 경로 세 곳(무결과 · 합의부족 · 점수미달) 모두에서, 이전 검증
결과에 `product_url` 이 있으면 그 값을 유지하고 미채택 사유만
`reverify_note` 로 남긴다. 이전 값은 통합본
(`hwahae_verified_39.json`)에서 읽는다 — 재검증은 결과 파일을 비우고
시작하므로 results 만으로는 알 수 없기 때문이다.

이 장치가 있었다면 3-13 사고 자체가 일어나지 않았다.

---

## 4. 제형 판정의 함정

### 4-1. 세부 제형이 상위어를 문자열로 포함한다

`'아이크림'` 안에 `'크림'` 이 들어있어 둘 다 잡힌다. 그대로 교집합을 보면
`아이크림`과 `크림`이 같은 것이 된다.
→ `_narrow_forms()` 로 세부 쪽이 있으면 상위어를 지운다.

### 4-2. 복합 표기는 뒤에 오는 말이 정체다

`'크림 마스크'` 는 크림이 아니라 마스크다. 앞의 말은 내용물 설명이다.
→ `COMPOUND_FORMS` 로 앞쪽 제형을 지운다.
실측: 셀리맥스 '브라이트닝 크림 35ml' 이 '브라이트닝 크림 마스크 4매'와
A등급으로 묶여 있었다.

### 4-3. 대분류/소분류는 포함관계로 봐야 한다

`'클렌징'` 과 `'클렌징 폼'` 은 같고, `'클렌징 폼'` 과 `'클렌징 오일'` 은
다르다. 대분류 이름이 소분류 문자열에 들어있어 단순 교집합으로는
구분되지 않는다.
→ 비교는 소분류로만 하고 대분류는 포함관계 판정에만 쓴다(`FORM_PARENTS`).

### 4-4. 자외선 차단 등급의 '+'는 등급 기호다

`'SPF50+ PA++++ 50ml'` 에서 마지막 `+` 와 용량이 붙어 `'+ 50ml'` 로 읽혀
멀쩡한 선크림이 세트로 잡혔다. 여기 쓰인 `+` 는 등급을 나타내는 기호이지
`'제품 A + 제품 B'` 의 `+` 가 아니다.

- PA 등급은 `PA+` ~ `PA++++` 네 단계
- SPF 는 숫자 뒤에 `+` 가 붙을 수 있다(`SPF50+` = 'SPF 50 이상')
- 구분자가 제각각이다. 실측 표기 형태:

```
SPF50+ PA++++     SPF 50+ PA++++    SPF50+/PA++++
SPF50+PA++++      SPF50+, PA++++    SPF50+ / PA++++
SPF38 PA++        SPF20 PA++        SPF50+
```

→ 세트 판정 전에 `SPF_NOTATION_RE` 로 먼저 지운다. 등급 표기가 있으면서
**동시에 진짜 세트인 경우**(`선크림 SPF50+ PA++++ 50ml + 선스틱 20g`)는
그대로 세트로 잡혀야 한다 — 회귀검사가 두 경우를 다 확인한다.

---

## 5. 브랜드 판정의 함정

### 5-1. 네이버 brand 칸은 판매처명인 경우가 많다

```
viviscal -> brand '슈퍼대디',   상품명 '비비스칼 프로 viviscal pro'
呂       -> brand '아모레퍼시픽', 상품명 '려 루트젠 두피에센스'
```

→ 상품명에서도 찾는다. 사전에 없어도 5자 이상이면 그대로 찾고, 4자는
상품명 앞부분일 때만(상품명은 대개 브랜드로 시작한다).
짧은 영문은 다른 단어에 우연히 들어간다('LOA'가 'FLOAT'에).

### 5-2. 영문 원본을 한글 판매처명과 직접 비교할 수 없다

`LE LABO` vs `르라보` 는 알파벳만 남기면 절대 안 맞는다. 가나는 판단불가로
빠지는데 영문만 빠져 있어 불일치 813건 중 245건(30%)이 이 유형이었다.
→ 판매처명에 라틴문자가 없으면 `unknown`.

### 5-3. 로컬 수집 브랜드가 가장 정확하다

`data/collected_pages.json` 의 brand 는 상품 페이지에서 직접 읽은 값이다.
이걸로 대조해 오매칭 187건을 찾았다(나이키 운동화·후지필름 카메라·
프라모델까지 검수 목록에 있었다).

---

## 6. 제외 기준의 변천

### 6-1. '원산지'가 아니라 '국내 유통 여부'

처음엔 일본·유럽 브랜드를 통째로 뺐는데 기준이 잘못됐다. 이 사업은
한국에서 사서 큐텐재팬에 파는 구조라, 해외 브랜드라도 국내 유통이 있으면
대상이다. 비오더마·라로슈포제·큐렐·케라스타즈는 국내 약국·올리브영에서
정상 유통된다.
→ `foreign_brands.json` 에는 **국내 유통이 없는** 34종만 남긴다.

### 6-2. 가타카나라고 해외 브랜드가 아니다

`アヌア`=아누아, `メディキューブ`=메디큐브, `雪花秀`=설화수, `呂`=려는
전부 한국 브랜드다. 문자 종류로 자르면 한국 브랜드 수백 건이 사라진다.
→ 목록으로 관리하고, 회귀검사가 이 대표 브랜드들이 섞이지 않았는지 본다.

### 6-3. 목록을 공백으로 쪼개면 다단어 브랜드가 깨진다

`'LE LABO'` 가 `'LE'` + `'LABO'` 로, `'Suntory Wellness'` 가
`'Suntory'` + `'Wellness'` 로 갈라져 있었다. 조각은 어떤 브랜드와도
일치하지 않아 **조용히 아무 일도 안 한다** — 걸러진 줄 알았던 상품이
그대로 들어왔다. C구간을 눈으로 보지 않았으면 못 찾았다.

---

## 7. 새 규칙을 만들 때 점검할 것

1. 같은 시도가 이 문서 2절에 이미 있는가
2. 영향 건수를 실측했는가. 오탐 비율은 얼마인가
3. 정상 매칭이 걸리는 대표 사례를 찾아봤는가
4. 판정 순서상 앞 단계에서 이미 걸러지지는 않는가(3절)
5. `simulation.py` 에 검사 항목을 추가했는가
6. 되돌릴 수 있게 원본 값을 남기는가
