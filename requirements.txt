# [v7.35.0] 반드시 정확한 버전으로 고정한다. 캐시 키가 이 파일의 해시라서,
#  >=1.50처럼 열어두면 pip가 매번 최신을 새로 받아도 캐시 키는 안 바뀐다.
#  그 결과 캐시엔 옛날 버전용 브라우저만 있는데 방금 깐 새 playwright가
#  그 브라우저를 못 찾아 "playwright install 하라"며 매번 죽었다(화해/
#  무신사 서브프로세스가 code=1로 전멸, 전체 재검증이 막힘). 버전을 올릴
#  땐 이 줄을 의도적으로 바꿔서 캐시를 강제로 새로 받게 해야 한다.
playwright==1.62.0
Pillow>=10.0
openpyxl>=3.1
pandas>=2.0
hangul-romanize>=0.1.0
rapidfuzz>=3.0
