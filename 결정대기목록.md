# 결정 대기 목록

작성 2026-07-28 · 파이프라인 v3.3.0

미결 항목만 적는다. 결정되면 여기서 지우고 인계문서에 반영한다.

---

## ~~① 수확 주기~~ — 해결: 수동 실행 (2026-07-28)

크론에 넣지 않는다. 필요할 때 직접 트리거한다.

    {"ref":"main","inputs":{"run_harvest_catalog":"true"}}

⚠️ 반드시 **발굴과 다른 트리거로** 켤 것 — 통합의 일시정지 로직이 run
전체를 취소하기 때문에, 같은 트리거로 켜면 발굴을 세우려다 수확까지 죽는다.

참고 수급: 하루 순감소 527개 / 재고 26,507개(50일치) / 수확 1곳당 35개
공급 → 하루 15곳 상당이면 균형. 미수확 8,407곳은 553일치.

---

## ~~② 자동통합 임계치~~ — 해결(500건, v3.5.0)

**배경** — "발굴이 N건 모이면 자동 통합" 구조로 전환하기로 함.
현재 코드에 미구현이라 새로 만들어야 한다.

| 선택지 | 하루 통합 횟수 | 엑셀 왕복 |
|---|---:|---|
| 1. 250건 | 약 1.1회 | 하루 1번 |
| 2. 100건 | 약 2.8회 | 하루 3번 |
| 3. 시간 기준(6시간 경과) | 4회 | 하루 4번 |

구현 시 반드시 포함할 것:
- 카운터를 워커 상태파일에 저장(재기동해도 이어지게)
- 트리거 직전 진행중/대기중 통합 run 확인(워커 2개가 동시에 넘길 때 중복 방지)
- 외부 크론 Body에 `run_merge_discovery`를 유지 — 안전망
  (`ensure_discovery_alive`)이 통합 트리거에 딸려서만 돌기 때문에, 빼면
  "발굴이 죽으면 통합이 안 불리고 → 안전망도 안 돌아 영영 죽은 채"가 된다.

---

## ~~③ 검증 타임아웃~~ — 해결: 220분 (2026-07-28, v4.1.1)

외부 크론이 4시간(240분) 주기인데 타임아웃도 240분이라 여유가 0이었다.
앞 실행이 꽉 채우면 다음 run이 큐에 쌓이고 대기열이 계속 길어진다
(실측: 06:00 크론이 pending으로 대기). 20분 여유를 뒀다.
평상시 물량(하루 234건)은 1시간이면 끝나므로 손해가 없다.

---

## ~~④ 한 단어 검색어 23.2%~~ — 해결: 측정 먼저 (2026-07-28, v4.2.0)

큐 순서를 추측으로 건드리면 멀쩡한 검색어까지 뒤로 밀린다. 데이터를
먼저 모은다. 발굴이 검색어를 처리할 때마다 토큰 수 구간별로 누적한다.

    keyword_stats = {
      "1":   {"keywords": n, "shops": n, "saved": n},
      "2":   {...}, "3-4": {...}, "5+": {...}
    }

discovery_state_<B>.json 안에 들어가며, 구간 4개 x 숫자 3개라 파일이
커지지 않는다. 재기동해도 이어서 누적된다.

판단 시점: 구간마다 검색어 500개 이상 쌓이면 '상점/검색어' 비율을 비교.
현재 전체 평균은 상점 1곳당 검색어 5.4개다. 한 단어 구간이 이보다
뚜렷하게 나쁘면 큐 순서를 뒤로 미룬다.

---

---

## 참고 — 이미 결정된 것

- 검증 워커 1 → 3 (v3.1.0)
- Exa는 보조 호출로만 (v3.2.0) — 무료 월 크레딧 1,428건 한계
- 무료 소스 추가: 다음 웹문서, 네이버 웹문서 (v3.3.0)
- 합의 정족수 2 유지 + 상품DB 최소 1곳 필수
- `ANTHROPIC_API_KEY` 시크릿 삭제
- 발굴과 검증은 **별도 트리거로** 병행 (브랜치가 달라 경합 없음)

---

## 참고 — 이후 결정된 것 (2026-07-28)

- 자동통합 임계치 500건 (v3.5.0)
- 번역요청서 200줄씩 분할, 키는 상품번호 (v3.4.x / v3.5.1)
- 외부 크론 2개: 매일 02:00 발굴 / 4시간마다 검증+안전망
- 검증 워커 3 -> 6, 전체 재검증 (v3.6.0)
- 검수 확정 -> 브랜드·표기 사전 자동 학습 (v3.7.0, 저장 시 자동 v3.8.0)
- 검수페이지 브랜드 확실도 순 정렬 + 경고 (v3.9.0)
- 네이버 재검색으로 링크 회수 (v4.0.0)
- 네이버 단독 통과 (v4.1.0)
- 신뢰 판매처 필터: 현행 유지 (완화하지 않음)
- 가격 이상치 탐지: 도입하지 않음
