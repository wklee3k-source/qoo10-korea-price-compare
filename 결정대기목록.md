# 결정 대기 목록

작성 2026-07-28 · 파이프라인 v3.3.0

미결 항목만 적는다. 결정되면 여기서 지우고 인계문서에 반영한다.

---

## ① 수확 주기

**배경** — 검색어 재고 26,507개 = 50일치. 하루 순감소 527개.
수확은 상점 1곳당 신규 검색어 35개를 공급하므로 **하루 15곳이면 수지가 맞는다.**
미수확 8,407곳은 553일치. 지금 6워커 풀가동은 필요량의 수십 배다.

| 선택지 | 필요 작업 |
|---|---|
| 1. 월 1회 몰아서 | 없음 (트리거만 분리) |
| 2. 주 1회 소량 | 수확에 "이번엔 N곳만" 상한 입력값 추가 |
| 3. 매일 소량 자동 | 상한 추가 + 트리거 분리 |

⚠️ 발굴과 수확을 같은 트리거로 켜면 안 된다 — 통합의 일시정지가 run 전체를
취소해서 수확까지 죽는다.

---

## ~~② 자동통합 임계치~~ — 해결(500건, v3.5.0)

**배경** — "발굴이 N건 모이면 자동 통합" 구조로 전환하기로 함.
현재 코드에 미구현이라 새로 만들어야 한다.

| 선택지 | 하루 통합 횟수 | 엑셀 왕복 |
|---|---:|---|
| 1. 250건 | 약 1.1회 | 하루 1번 |
| 2. 100건 | 약 2.8회 | 하루 3번 |
| 3. 시간 기준(6시간 경과) | 4회 | 하루 4번 |

구현 시 반드시 포함할 것:
- 카운터를 워커 상태파일에 저장(재기동해도 이어지게)
- 트리거 직전 진행중/대기중 통합 run 확인(워커 2개가 동시에 넘길 때 중복 방지)
- 외부 크론 Body에 `run_merge_discovery`를 유지 — 안전망
  (`ensure_discovery_alive`)이 통합 트리거에 딸려서만 돌기 때문에, 빼면
  "발굴이 죽으면 통합이 안 불리고 → 안전망도 안 돌아 영영 죽은 채"가 된다.

---

## ③ 검증 타임아웃 (크론 주기는 4시간으로 결정됨)

**배경** — 검증 잡 타임아웃 240분. 크론이 그보다 짧으면 이전 run이 도는 중에
새 run이 큐에 쌓이고 대기열이 계속 길어진다(2026-07-28 실측).

외부 크론은 4시간 주기로 정해졌다. 그런데 검증 잡 타임아웃이 정확히
240분(=4시간)이라 여유가 0이다. 앞 실행이 꽉 채우면 다음 run이 큐에
쌓인다(2026-07-28 실측으로 확인된 현상).

| 선택지 |
|---|
| 1. 타임아웃을 220분으로 낮춤 (20분 여유) |
| 2. 그대로 240분 — 가끔 큐에 쌓이는 건 감수 |

---

## ④ 한 단어 검색어 23.2% 처리

**배경** — 생성된 검색어 46,316개 중 10,727개가 한 단어(`パウダー` 등).
넓게 걸려 다양한 상점이 나올 수도, 대형 상점만 떠서 사이클이 버려질 수도 있다.
**아직 측정된 적 없음.**

| 선택지 |
|---|
| 1. 측정부터 — 발굴 로그에 검색어 길이별 상점 발굴 성공률 기록 |
| 2. 뒤로 미루기 — 두 단어 이상을 먼저 소진하도록 큐 순서만 조정 |
| 3. 그냥 둔다 — 검색어는 50일치 남았으니 다른 병목 우선 |

---

## 참고 — 이미 결정된 것

- 검증 워커 1 → 3 (v3.1.0)
- Exa는 보조 호출로만 (v3.2.0) — 무료 월 크레딧 1,428건 한계
- 무료 소스 추가: 다음 웹문서, 네이버 웹문서 (v3.3.0)
- 합의 정족수 2 유지 + 상품DB 최소 1곳 필수
- `ANTHROPIC_API_KEY` 시크릿 삭제
- 발굴과 검증은 **별도 트리거로** 병행 (브랜치가 달라 경합 없음)

---

## 참고 — 이후 결정된 것 (2026-07-28)

- 자동통합 임계치 500건 (v3.5.0)
- 번역요청서 200줄씩 분할, 키는 상품번호 (v3.4.x / v3.5.1)
- 외부 크론 2개: 매일 02:00 발굴 / 4시간마다 검증+안전망
- 검증 워커 3 -> 6, 전체 재검증 (v3.6.0)
- 검수 확정 -> 브랜드·표기 사전 자동 학습 (v3.7.0, 저장 시 자동 v3.8.0)
- 검수페이지 브랜드 확실도 순 정렬 + 경고 (v3.9.0)
- 네이버 재검색으로 링크 회수 (v4.0.0)
- 네이버 단독 통과 (v4.1.0)
- 신뢰 판매처 필터: 현행 유지 (완화하지 않음)
- 가격 이상치 탐지: 도입하지 않음
