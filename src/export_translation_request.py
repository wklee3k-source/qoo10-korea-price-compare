"""export_translation_request.py — 미번역 상품을 '그대로 붙여넣으면 되는'
마크다운 한 장으로 뽑는다.

[왜 만들었나] 기존 엑셀 왕복은 (1) 엑셀 다운로드 (2) 열 맞춰 붙여넣기
(3) 번역열 채우기 (4) 로컬 파이썬 실행 순서라, 번역 자체보다 앞뒤가
더 번거로웠다. 번역은 어차피 다른 Claude 창에서 하므로, 지시문까지
파일 안에 넣어두면 사용자는 **파일 전체를 복사해서 붙여넣기만** 하면 된다.

출력 형식은 `번호|일본어명` 한 줄짜리로 고정한다. 표(마크다운 테이블)로
주면 돌려받을 때 파이프와 정렬이 깨져서 파싱이 자주 실패한다.

사용법:
    python export_translation_request.py ../output/discovery_state.json \\
        ../output/번역요청.md [최대건수]
"""
import json
import re
import sys
from pathlib import Path

HANGUL_RE = re.compile(r"[가-힣]")

# 번역 지시문. 화장품 도메인에서 반복적으로 틀리는 것들을 명시한다 —
# 예전 API 번역에서 실제로 나왔던 오역들이다.
INSTRUCTION = """아래는 큐텐재팬에 올라온 **한국 화장품**의 일본어 상품명 목록입니다.
각 줄을 한국어 상품명으로 옮겨 주세요.

**이 번역이 어디에 쓰이는지 알고 시작해 주세요**
번역 결과는 화면에 보여주려는 게 아니라, **네이버 쇼핑에서 같은 상품을
찾는 검색어**로 그대로 들어갑니다. 그래서 "일본어를 한국어로 옮긴 말"이
아니라 **"한국 쇼핑몰에 실제로 그렇게 적혀 있는 이름"**이어야 합니다.
매끄러운 번역보다 실제 표기가 우선입니다.

**규칙**
1. 출력은 `상품번호|한국어명` 형식 한 줄씩. 설명·인사·머리말 없이 목록만.
2. 맨 앞 상품번호(10자리 숫자)는 **한 글자도 바꾸지 말고** 그대로.
   순서를 바꾸거나 줄을 빼지 마세요.
3. **입력 줄의 `[대괄호]`는 이 상품의 브랜드입니다.** 발굴 단계에서
   확인해둔 값이라 신뢰할 수 있습니다. 상품명을 풀 때 이 브랜드를
   근거로 삼으세요 — 예를 들어 `[ビープレーン]`이 있으면 비플레인
   제품이라는 뜻이고, 뒤따르는 제품명도 그 브랜드 라인에 맞춰
   해석하면 정확해집니다.
   **출력에는 대괄호를 그대로 쓰지 말고**, 한국 쇼핑몰 표기대로
   브랜드명을 상품명 앞에 자연스럽게 붙이거나(예: `비플레인 녹두
   약산성 클렌징폼 80ml`), 원문 상품명에 이미 브랜드가 들어있으면
   중복해서 넣지 마세요.
4. **있는 그대로 전부 옮기세요.** 프로모션 문구, 증정·세트 구성(1+1, 2+1),
   배송 문구, 괄호 안 설명, 기호까지 빼지 말고 원래 순서대로. 요약 금지.
5. 제품명은 **한국에서 실제로 쓰는 이름을 복원**하세요. 직역하지 마세요.
6. 브랜드명은 한국 공식 표기로 (アヌア→아누아, メディキューブ→메디큐브).
7. 용량·수량(50ml, 2매입)은 그대로.
8. **라인명·번호·색상·제형을 절대 바꾸거나 빼지 마세요.** 같은 브랜드
   안에서 이것만 다른 제품이 많아, 하나만 틀려도 다른 상품이 됩니다.
       5번 글루타치온 ≠ 1번 판토텐산
       화이트 블론드 ≠ 퍼플
       에센스 ≠ 크림 ≠ 젤크림 ≠ 밤
9. 확실하지 않으면 지어내지 말고 `번호|` 로 비워 두세요.
   빈 줄은 다음 회차에 다시 시도됩니다. 틀린 값이 들어가면 '번역완료'로
   굳어 영영 고쳐지지 않으니, 비우는 편이 낫습니다.
10. **목록을 다 옮긴 뒤, 맨 아래에 피드백을 아주 상세히 적어 주세요.**
   아래 구분선을 그대로 쓰고 그 뒤에 적으면 됩니다. 구분선 아래는
   기계가 읽지 않으니 형식·분량에 얽매이지 마세요. **길수록 좋습니다.**

   ```
   ---피드백---
   ```

   이 목록을 옮기려면 상품 하나하나를 찾아보게 됩니다. 그 과정에서
   알게 된 것들이 이 사업에서 가장 값진 정보인데, 적어두지 않으면
   이 창이 닫히는 순간 전부 사라집니다. **번역 결과보다 이 피드백이
   더 오래 남습니다.**

   ### 반드시 적어 주세요

   **(1) 찾아보고 알아낸 브랜드 표기**
   일본어·영문 브랜드를 한국에서 실제로 뭐라고 부르는지 확인했다면
   전부 적어 주세요. 확인한 근거(어디서 봤는지)도 함께요.
   ```
   ミルボン → 밀본 (올리브영·네이버에서 '밀본'으로 판매)
   ululis → 우루리스 (한국 미판매, 일본 브랜드)
   La sana → 라사나 (쿠팡 표기 '라사나')
   ```

   **(2) 라인명·시리즈 체계**
   같은 브랜드 안에서 제품을 가르는 이름 규칙을 알아냈다면 적어 주세요.
   ```
   넘버즈인은 라인이 번호로 구분됨. 1번=시카, 3번=모공, 4번=콜라겐,
   5번=글루타치온. 번호를 빼면 완전히 다른 제품이 됨.
   아누아는 어성초77/어성초80/라이스70 처럼 성분+숫자로 라인 구분.
   ```

   **(3) 이 상품이 한국에서 어떤 상태인지**
   찾아보다 알게 된 것을 적어 주세요. 검증 단계에서 그대로 쓰입니다.
   - 한국에서는 안 파는 것 같다 (일본·중국·유럽 브랜드)
   - 단종된 것 같다 / 리뉴얼돼 이름이 바뀐 것 같다
   - 한국에서는 다른 이름으로 판다 (예전 이름 → 새 이름)
   - 화장품이 아니다 (영양제·잡화·가전 등)

   **(4) 망설인 지점 전부**
   ```
   医薬部外品: 한국엔 없는 제도라 망설였음. 그대로 "의약부외품"으로 옮김.
   詰め替え: "리필용"과 "교체용" 중 리필용이 더 흔해 보여 통일함.
   ○○という単語: 검색해도 한국 표기를 못 찾아 음차함.
   ```
   어떻게 옮겼는지, 왜 그렇게 정했는지까지 적어 주세요.
   **틀렸을지도 모른다고 생각한 것일수록 꼭 적어 주세요.**

   **(5) 여러 번 나온 패턴**
   같은 고민이 반복됐다면 그게 규칙이 될 후보입니다.
   ```
   "サロン専売"가 12번 나왔음. 전부 "살롱전용"으로 통일.
   용량이 "1000ml/g"처럼 섞인 표기가 8건. 원문 그대로 뒀음.
   ```

   **(6) 비운 줄은 하나하나 이유를 적어 주세요**
   같은 빈칸이라도 이유에 따라 손볼 곳이 완전히 다릅니다.
   ```
   1234567890 — 브랜드를 못 찾음. 검색해도 안 나옴.
   1234567891 — 상품명이 중간에 잘려 있어 무슨 제품인지 알 수 없음.
   1234567892 — 화장품이 아니라 커피믹스임.
   1234567893 — 일본 전용 상품으로 보임. 한국 판매 흔적 없음.
   ```

   **(7) 규칙이 서로 부딪힌 곳**
   어느 쪽을 따랐는지, 왜 그랬는지 적어 주세요.

   **(8) 이 지시문에서 고쳤으면 하는 것**
   설명이 부족했던 부분, 예시가 필요한 부분, 순서가 헷갈린 부분.

   ### 적을 때

   - **사소해 보여도 적어 주세요.** 판단하지 말고 다 적는 편이 낫습니다.
     "이건 당연한 건데" 싶은 것이 다음 사람에겐 처음 겪는 벽입니다.
   - 확신이 없어도 적어 주세요. "아마 ○○인 것 같다"도 단서가 됩니다.
   - 상품번호를 함께 적어 주면 나중에 찾아보기 좋습니다.

   이 피드백은 번역 규칙집에 그대로 반영됩니다. 다음 회차부터는
   이 내용이 규칙에 들어간 채로 시작하게 됩니다.

**★ 가장 중요: 제형(제품 형태)을 정확히 옮기세요 ★**
제형이 틀리면 다른 상품이 됩니다. 같은 브랜드·같은 라인이라도 크림과
에센스는 다른 제품이고, 클렌징 폼과 클렌징 오일도 다른 제품입니다.
제형 단어는 **한국 쇼핑몰에 적혀 있는 그대로** 써 주세요.

| 일본어 | 한국 표기 |
|---|---|
| 化粧水 / トナー | 토너 |
| 乳液 | 로션 |
| 美容液 / セラム | 에센스 · 세럼 |
| アンプル | 앰플 |
| クリーム | 크림 |
| ジェルクリーム | 젤크림 |
| ジェル | 젤 |
| バーム | 밤 |
| ミスト | 미스트 |
| パック / マスク / シートマスク | 마스크팩 |
| 鼻パック | 코팩 |
| トナーパッド / 拭き取りパッド | 토너패드 |
| アイパッチ | 아이패치 |
| 洗顔料 / 洗顔フォーム | 클렌징폼 |
| クレンジングオイル | 클렌징오일 |
| クレンジングバーム | 클렌징밤 |
| クレンジングウォーター | 클렌징워터 |
| クレンジングミルク | 클렌징밀크 |
| メイク落とし / リムーバー | 리무버 |
| スクラブ / ピーリング / ゴマージュ | 스크럽 |
| 日焼け止め / UVクリーム | 선크림 |
| UVエッセンス / サンセラム | 선세럼 |
| サンスティック | 선스틱 |
| サンクッション | 선쿠션 |
| シャンプー | 샴푸 |
| コンディショナー / リンス | 컨디셔너 |
| トリートメント / ヘアパック | 트리트먼트 |
| ヘアオイル | 헤어오일 |
| ヘアエッセンス / ヘアセラム | 헤어에센스 |
| ヘアミスト / ヘアパフューム | 헤어미스트 |
| ヘアワックス / ポマード | 왁스 |
| ヘアカラー / 白髪染め | 염색약 |
| 頭皮エッセンス / スカルプトニック | 두피에센스 |
| ボディソープ / シャワージェル | 바디워시 |
| ボディローション | 바디로션 |
| ボディクリーム / ボディバター | 바디크림 |
| ハンドクリーム | 핸드크림 |
| アイクリーム | 아이크림 |
| 制汗剤 / デオドラント | 데오도란트 |
| 除毛剤 / 脱毛 | 제모제 |
| 入浴剤 | 입욕제 |
| ブラシ / くし | 브러시 |

제형이 상품명에 없으면 억지로 넣지 마세요. 다만 일본어 상품명에 있으면
반드시 옮겨야 합니다 — 빠뜨리면 검색이 엉뚱한 제형으로 갑니다.

**카테고리 용어 — 일본식 한자어를 쓰지 마세요**
- 美容液 → 에센스 (○) / 미용액 (×)
- 化粧水 → 토너 (○) / 화장수 (×)
- 乳液 → 로션 (○) / 유액 (×)
- 洗顔料 → 클렌저 · 클렌징폼 (○) / 세안료 (×)
- 日焼け止め → 선크림 (○)
- シートマスク → 마스크팩 (○) / 시트 마스크 (×)
- スージング → 수딩 (○) / 수징 (×)
- エクソソーム → 엑소좀 (○) / 엑소솜 (×)
- ブレミッシュ → 블레미쉬 (○) / 블레미시 (×)
- 두피 · 미백 · 모공 · 히알루론산 · 자외선 차단제는 그대로
  (한국에서도 그렇게 씁니다. 억지로 바꾸지 마세요)

**붙여 쓰는 말 — 한국 상품명은 대부분 붙여 씁니다**
선크림 · 선스틱 · 선세럼 · 선쿠션 · 아이크림 · 젤크림 · 수분크림
토너패드 · 마스크팩 · 클렌징폼 · 클렌징오일 · 클렌징워터
바디로션 · 바디워시 · 바디크림 · 헤어오일 · 헤어에센스 · 헤어팩

**자주 나오는 말**
- ドクダミ → 어성초 (○) / 삼백초 (×)
- ツボクサ → 병풀,  シカ / CICA → 시카 (○) / 사슴 (×)
- ヨモギ → 쑥,  コメ / 米 → 쌀,  ハトムギ → 율무
- 桃 / ピーチ → 복숭아,  緑茶 → 녹차,  緑豆 → 녹두
- ヒアルロン酸 → 히알루론산,  セラミド → 세라마이드
- ナイアシンアミド → 나이아신아마이드,  グルタチオン → 글루타치온
- コラーゲン → 콜라겐,  レチノール → 레티놀,  ペプチド → 펩타이드
- ニキビ → 트러블 (○) / 여드름도 가능,  トーンアップ → 톤업
- レフィル → 리필,  アンプル → 앰플,  セラム → 세럼
- エイジングケア → 안티에이징,  ダメージケア → 데미지케어
- バクチオール → 바쿠치올 (○, 히라가나 발음 그대로 옮기면 '박토올·박치올' 같은
  오역이 잦다 — 성분 정식 표기 '바쿠치올'로 옮길 것)
"""



def _load_foreign_brands(state_path: Path) -> set:
    """해외브랜드 목록을 읽는다. 못 읽으면 빈 집합(=아무것도 안 뺀다).

    목록을 못 읽었을 때 전부 번역 대상으로 두는 쪽이 안전하다.
    반대로 하면 파일 하나 없어졌다고 번역이 통째로 멈춘다.
    """
    for candidate in (state_path.parent.parent / "data" / "foreign_brands.json",
                      Path("data/foreign_brands.json"),
                      Path("../data/foreign_brands.json")):
        try:
            return set(json.loads(candidate.read_text(encoding="utf-8")))
        except (OSError, ValueError):
            continue
    print("[경고] 해외브랜드 목록을 못 읽음 — 전부 번역 대상으로 둔다")
    return set()


def export(state_path: str, out_path: str, limit: int | None = None,
           single_file: bool = True,
           chunk: int = 200) -> int:
    """미번역 목록을 요청서로 뽑는다.

    기본은 **파일 하나**에 전부 담고 chunk건씩 묶음으로 나눠 적는다.
    받는 쪽이 한 묶음씩 끊어서 답하므로, 사장님은 파일 하나만
    붙여넣고 "계속"만 누르면 된다(v7.72.0).

    `--split`을 주면 예전처럼 파일을 여러 장으로 자른다.

    [왜 자르나] 500줄을 한 장으로 주면 입력은 문제없지만(약 32k 토큰)
    **응답이 27k 토큰**이 된다. 이 길이는 한 번의 답변에서 잘리거나
    중간부터 번호가 어긋나기 쉽다. 200줄이면 응답이 약 11k 토큰이라
    안전하고, 중간에 실패해도 그 장만 다시 하면 된다.
    """
    path = Path(state_path)
    if not path.exists():
        print(f"[SKIP] {path} 없음")
        return 0

    state = json.loads(path.read_text(encoding="utf-8"))
    products = state.get("all_products", [])
    pending = [p for p in products if not p.get("translated_kr")]

    # [v7.68.0] 한국에서 안 파는 브랜드는 번역 요청서에서 뺀다.
    #
    # [왜 — 실측 2026-08-16] 지금까지 번역한 6,168건 중 1,340건(21.7%)이
    # 해외브랜드였다. 사장님이 번역한 다섯 건 중 하나는 애초에 쓸 데가
    # 없는 것이었다. 이들은 번역해도 검증 대상에서 빠지므로 검수페이지에
    # 오르지 않는다 — 한국에서 사서 일본에 파는 사업이기 때문이다.
    #
    # 08회차 회신을 보면 번역하는 쪽도 이미 눈치채고 있었다:
    # "菊星는 일본 미용실 전용, 한국 판매 흔적이 전혀 없어 비웠음".
    # 그 판단을 사람이 매번 할 게 아니라 애초에 목록에서 빼는 게 맞다.
    #
    # [뺀 것을 되돌릴 수 있어야 한다] 해외브랜드 판정이 틀렸을 때
    # 그 상품이 영영 번역되지 않으면 손해다. 그래서 지우지 않고
    # 건너뛰기만 하며, 몇 건을 왜 뺐는지 로그에 남긴다.
    # 해외브랜드 목록에서 빼면 다음 회차에 자동으로 다시 올라온다.
    foreign = _load_foreign_brands(path)
    if foreign:
        before = len(pending)
        pending = [p for p in pending
                   if (p.get("brand") or "").strip() not in foreign]
        skipped = before - len(pending)
        if skipped:
            print(f"[제외] 한국 미판매 브랜드 {skipped}건 — 번역 대상 아님 "
                  f"(해외브랜드 목록 {len(foreign)}개 기준)")

    total_pending = len(pending)
    if limit:
        pending = pending[:limit]

    print(f"[INFO] 전체 {len(products)}건 중 번역 대상 {total_pending}건")

    out = Path(out_path)
    stem, suffix = out.stem, out.suffix or ".md"
    # 이전 회차 파일을 먼저 지운다 — 안 지우면 이미 끝난 목록이 남아
    # 다시 번역하게 되고, 장수가 줄었을 때 옛 뒷장이 유령처럼 남는다.
    for old_file in out.parent.glob(f"{stem}*{suffix}"):
        old_file.unlink()

    if not pending:
        print("[INFO] 미번역 상품 없음 — 번역요청 파일 생성 생략")
        return 0

    chunks = [pending[i:i + chunk] for i in range(0, len(pending), chunk)]
    n_files = len(chunks)

    # [v7.72.0] 파일 하나에 전부 담고, 받는 쪽이 나눠서 처리하게 한다.
    #
    # [왜 — 사장님 요청 2026-08-16] 예전엔 200건씩 잘라 파일 여러 장을
    # 만들었다. 한 번에 다 주면 번역하는 창이 멈추기 때문이다. 그런데
    # 장이 10장씩 되니 파일을 열 번 복사해 붙여넣어야 했다.
    #
    # 파일은 하나로 주되 목록을 200건 단위 묶음으로 나눠 적고, 지시문에
    # "한 번에 한 묶음씩 답하라"고 쓴다. 그러면 사장님은 파일 하나만
    # 붙여넣고 "계속"만 누르면 된다.
    if single_file:
        lines = [
            "# 번역 요청",
            "",
            f"총 **{total_pending}건**입니다. "
            f"{chunk}건씩 **{n_files}묶음**으로 나눠 두었습니다.",
            "",
            "**이 파일 전체를 복사해서 다른 Claude 창에 붙여넣으세요.**",
            "",
            "---",
            "",
            "## 진행 방법",
            "",
            "1. **묶음 1**을 옮기고, 그 결과만 먼저 내놓으세요.",
            "2. '계속'이라고 하면 **다음 묶음**을 하세요.",
            "3. 다만 **짝수 묶음(2, 4, 6...)을 끝냈을 때는** 그 직전 묶음까지",
            "   **두 묶음을 합쳐서** 한 번에 내놓으세요.",
            "   (묶음 2를 끝내면 1+2를, 묶음 4를 끝내면 3+4를 합쳐서)",
            f"4. **묶음 {n_files}**까지 끝나면 마지막에 피드백을 적으세요.",
            "",
            "**왜 두 묶음씩 합치나** — 사장님이 결과를 받아 저장하는데, 묶음마다",
            "따로 주면 복사할 게 많아지고 중간에 빠뜨리기 쉽습니다.",
            "두 묶음(약 400건)이 한 번에 내놓을 수 있는 최대치입니다.",
            "",
            "**그보다 더 모으려고 하지 마세요.** 세 묶음 이상을 합치면 답변 도중에",
            "잘려서 그때까지 한 것도 못 받습니다. 실제로 이 문제로 여러 번",
            "작업이 멈췄습니다.",
            "",
            "**앞 묶음을 다시 번역하지 마세요.** 합쳐서 내놓을 때는 이미 옮긴",
            "그대로 붙여서 내면 됩니다.",
            "",
            "결과 맨 앞에 어느 묶음인지 표시하세요. 예: `=== 묶음 1~2 ===`",
            "그 아래는 다른 말 없이 목록만 내놓으세요.",
            "",
            "---",
            "",
            INSTRUCTION,
            "",
        ]
        base_no = 0
        index_map = {}
        for idx, part in enumerate(chunks, 1):
            lines += [f"## 묶음 {idx} / {n_files}  ({len(part)}건)", "", "```"]
            for i, pr in enumerate(part, base_no + 1):
                title = (pr.get("title") or "").replace("\n", " ").replace("|", "/").strip()
                brand = (pr.get("brand") or "").replace("\n", " ").replace("|", "/").strip()
                if brand:
                    lines.append(f"{pr.get('goods_no')}|[{brand}] {title}")
                else:
                    lines.append(f"{pr.get('goods_no')}|{title}")
                index_map[i] = pr.get("goods_no")
            lines += ["```", ""]
            base_no += len(part)
        pairs = []
        for _s in range(1, n_files + 1, 2):
            _e = min(_s + 1, n_files)
            pairs.append(f"묶음 {_s}~{_e}" if _e > _s else f"묶음 {_s}")
        lines += [
            "---",
            "",
            "## 다 끝냈는지 확인",
            "",
            "아래를 전부 내놓았는지 마지막에 확인하세요. 빠진 게 있으면 그것부터 하세요.",
            "",
        ] + [f"- [ ] {x}" for x in pairs] + [
            "- [ ] 피드백",
            "",
            "---",
            "",
            "<!-- 번호↔상품번호 대응표. 반영할 때 쓰므로 지우지 마세요. -->",
            "<!-- INDEX_MAP " + json.dumps(index_map, ensure_ascii=False) + " -->",
            "",
        ]
        name = out.parent / f"{stem}{suffix}"
        name.write_text("\n".join(lines), encoding="utf-8")
        ja_only = sum(1 for pr in pending if not HANGUL_RE.search(pr.get("title") or ""))
        print(f"[OK] {name.name} — {total_pending}건 / {n_files}묶음 (일본어만 {ja_only}건)")
        return 0

    base_no = 0
    for idx, part in enumerate(chunks, 1):
        lines = [
            f"# 번역 요청 {idx}/{n_files}",
            "",
            f"미번역 {total_pending}건 중 이 장은 {len(part)}건. "
            "**이 파일 전체를 복사해서 다른 Claude 창에 붙여넣으세요.**",
            "",
            "---",
            "",
            INSTRUCTION,
            "",
            "**목록**",
            "",
            "```",
        ]
        index_map = {}
        for i, p in enumerate(part, base_no + 1):
            title = (p.get("title") or "").replace("\n", " ").replace("|", "/").strip()
            # [v7.48.9] 브랜드를 함께 준다. 발굴 단계에서 이미 확보해둔
            # 값인데 요청서에는 안 넣고 있었다 — 브랜드를 알면 상품명
            # 해석이 훨씬 정확해진다(예: "ビープレーン"이 비플레인이라는
            # 걸 알면 뒤따르는 제품명도 제대로 풀린다). 브랜드가 없으면
            # 예전처럼 상품명만 준다.
            brand = (p.get("brand") or "").replace("\n", " ").replace("|", "/").strip()
            # [v3.5.1] 키를 순번이 아니라 상품번호로 쓴다. 순번을 쓰면,
            # 사용자가 번역하는 동안 통합이 한 번 더 돌아 요청서가
            # 새로 만들어졌을 때 번호가 밀려 엉뚱한 상품에 이름이 박힌다
            # (안전망 통합이 4시간마다 도는 운영에서는 실제로 일어난다).
            if brand:
                lines.append(f"{p.get('goods_no')}|[{brand}] {title}")
            else:
                lines.append(f"{p.get('goods_no')}|{title}")
            index_map[i] = p.get("goods_no")
        lines += [
            "```",
            "",
            "---",
            "",
            "<!-- 번호↔상품번호 대응표. 반영할 때 쓰므로 지우지 마세요. -->",
            "<!-- INDEX_MAP " + json.dumps(index_map, ensure_ascii=False) + " -->",
            "",
        ]
        name = out.parent / (f"{stem}_{idx:02d}{suffix}" if n_files > 1 else f"{stem}{suffix}")
        name.write_text("\n".join(lines), encoding="utf-8")
        print(f"[OK] {name.name} — {len(part)}건")
        base_no += len(part)

    ja_only = sum(1 for p in pending if not HANGUL_RE.search(p.get("title") or ""))
    print(f"[완료] {n_files}장 / 총 {len(pending)}건 (일본어만 {ja_only}건)")
    return len(pending)


if __name__ == "__main__":
    if len(sys.argv) < 3:
        print(__doc__)
        raise SystemExit(1)
    lim = int(sys.argv[3]) if len(sys.argv) > 3 and sys.argv[3].strip() else None
    ch = int(sys.argv[4]) if len(sys.argv) > 4 and sys.argv[4].strip() else 200
    single = "--split" not in sys.argv
    export(sys.argv[1], sys.argv[2], lim, single, ch)
