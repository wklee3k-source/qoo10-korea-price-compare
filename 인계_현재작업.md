# 인계 — 큐텐재팬 한국화장품 소싱 파이프라인 (v7.48.9)

저장소: `wklee3k-source/qoo10-korea-price-compare`
검수페이지: https://wklee3k-source.github.io/qoo10-korea-price-compare/

---

## 0. 새 창에서 시작할 때 먼저 할 일

1. 저장소를 `--depth 1` 얕은 클론으로 받는다(`.git` 이 2.7GB 로 부푼 사고가 있었다)
2. **`판정로직_설계이력.md` 를 반드시 먼저 읽는다** — 시도했다가 되돌린
   방법과 그 근거(실측 숫자)가 모여 있다. 안 읽으면 같은 시도를 반복한다
3. 관련 스킬을 확인한다 — `qoo10-page-collector`(로컬 수집기),
   `qoo10-translation`(번역). 이름이 나오면 스킬이 자동으로 열린다
4. 배포 전 `python simulation.py` 가 반드시 통과해야 한다(현재 82항목)
5. **GitHub 푸시가 필요하면 토큰이 없다.** 사장님이 전 대화창에서 토큰을
   찾아 붙여넣어 준 적이 있다 — `conversation_search`로 "GitHub token"
   검색해서 찾을 것. 못 찾으면 사장님께 요청한다

---

## 1. 지금 상태 — 전체 재검증 진행중 + 번역 940건 회신 대기 (2026-08-15 00:54 KST)

**바로 이어서 할 일 두 가지**

1. **번역 940건 회신 받아 반영** — `번역요청_06_01~05.md`(5장)를 사장님이
   다른 창에서 번역중. 회신이 오면 `import_translation_response.py`로
   반영하고 discovery-live에 커밋한다. 이 요청서는 **브랜드 포함**
   형식(`상품번호|[브랜드] 일본어명`)으로 새로 만든 것이다(v7.48.9).
2. **전체 재검증 완료 확인** — 검증 오매칭 버그 2건을 고친 뒤(v7.48.6~7)
   검증 결과를 전부 비우고 0부터 재판정중. 00:54 기준 220/5,504건 처리.
   완료되면 로컬 수집기용 보완 대상을 다시 뽑는다
   (`export_collector_targets.py`).

**백업**: 재검증 초기화 직전 상태가 `hwahae-live-backup-20260814-full`
브랜치에 통째로 보존돼 있다(로컬 수집 결과 상품명 2,796건·가격 2,638건·
사진 2,102건 반영본 포함).

**크론 2개로 자동화 완료**: 수집크론(2시간, 발굴+수확) + 검증크론(4시간).

```
수집크론 — 2시간마다
{"ref":"main","inputs":{"discovery_parallel":"true","run_harvest_catalog":"true"}}
```

**데이터 현황(00:54 KST)**: 통합본 5,545 / 번역완료 5,514(미번역 31) /
검증대상 5,504 / 재검증 완료 220 · 남음 5,284 / 수확 521,121.

### 1.1 실측 사고 — 발굴·수확 완전정지, 아무것도 못 깨움 (2026-08-13~14)

발굴·수확이 5시간 타임아웃으로 완전히 죽었는데, 다시 깨워줄 게 시스템
안에 하나도 없었다. `ensure_discovery_alive`/`ensure_harvest_alive`
안전장치는 전부 **병합job 안에서만** 실행되는데(needs:
merge_discovery_shards / merge_fullcatalog_shards), 병합 자체가
발굴이 살아있어야 걸리는 임계치 트리거로만 열리는 구조였다. 발굴이
완전히 죽으면 병합도 안 걸리고, 병합이 안 걸리면 안전장치도 같이
죽는 모순 — "죽었는지 확인해주는 장치"가 정작 죽어야 하는 순간에
자기도 같이 죽었다.

**조치(v7.46.0)**: 두 안전장치(ensure_discovery_alive,
ensure_harvest_alive) 폐기. 내부 상태와 무관하게 무조건 주기적으로
발굴+수확을 켜는 외부 수집크론으로 대체. simulation.py #20(이 job의
존재를 요구하던 검사)도 같이 폐기.

### 1.2 왜 발굴병합·수확병합·크론A가 다 필요없는지

- **발굴병합**: 발굴 워커 안에 자체 임계치 로직이 있다(500건 쌓이면
  스스로 병합 호출). 발굴이 살아있는 한 이미 자동이다.
- **수확병합**: 수확은 검수 대상이 아니라 발굴 검색어 재료로만 쓴다
  (`build_review.py`에서 이미 제외됨). 재료로 쓰는
  `refill_discovery_keywords.py`가 `fullcatalog_state*.json` 패턴으로
  샤드 파일을 직접 읽어서, 병합된 파일이 아예 없어도 지장 없다 —
  병합 자체가 불필요하다.
- **크론A(수확 재기동 백업)**: ensure_harvest_alive가 있었을 땐
  이걸로 대체 가능하다고 봤지만, §1.1 사고로 그 전제가 깨졌다. 지금은
  크론A 역할을 "수집크론"이 흡수한다 — 발굴+수확을 한 바디로 같이
  깨우므로 별도로 안 둔다.

### 1.3 실측 사고 — 병합 기준선이 유령 샤드까지 셈 (2026-08-14)

워커를 10개→6개로 줄이면서 예전 6~9번 워커 샤드 파일
(`discovery_state_6~9.json`, 1,829건)을 안 지웠다. 기준선 계산이
`glob('discovery_state_*.json')`로 전부 긁어모아서 이 죽은 파일까지
같이 세는 바람에, 기준선이 실제(2,887)보다 1,829건 부풀어(4,716)
자동통합 임계치(500건)가 사실상 영원히 안 걸리는 상태였다.

**조치(v7.46.1)**: 유령 샤드 4개 삭제, 기준선·신규건수 계산 둘 다
워커수(6)로 범위 고정. 워커수 또 바뀌면 이 숫자(6)도 코드에서 같이
고칠 것 — 안 고치면 이 사고가 재발한다.

---

## 2. 이번 세션 주요 변경 (v7.25.0 ~ v7.48.9)

버전이 아주 많이 올라갔다. 핵심만 요약한다 — 상세 근거는 전부
`판정로직_설계이력.md` 3-1 ~ 3-21 에 있다.

| 구간 | 내용 |
|---|---|
| v7.25~7.32 | 수량·용량·증정품 판정 로직 대거 보강. 본품/증정/타제품 3분류, 큐텐측 증정 초과분 삭제, 대소문자 무시, 더블팩 인식 |
| v7.33~7.34 | 브랜드 사전 영문 별칭 17종, 공백 무시 비교 |
| v7.35 | playwright 버전 고정(캐시 무효화 수단으로) |
| v7.36 | 메이크업 픽서·세팅스프레이 제외(색조 부속) |
| v7.37 | 네이버쇼핑 API 영구 종료(2026-07-31) 대응 — 검증 실패가 기존 값을 지우지 않도록 보호(근본 안전장치) |
| v7.38 | 네이버 웹문서를 NAVER API HUB 로 이관 |
| v7.39 | Exa 무료 한도(월 1,400회) 자동 준수 |
| v7.40 | 구매링크를 네이버 웹문서로 대체 확보(오픈마켓 위주) |
| v7.41 | 로컬 크롬 검색으로 스마트스토어 링크 확보(수집기 v2.3.x 검색 모드 신설) |
| v7.42 | 오픈마켓·가격비교 제외, 정품 채널만 사용 |
| v7.43 | 검수 카드에 공식스토어 배지 |
| v7.44.0 | 발굴·수확 워커 10개로 증설(검증과 동일) |
| v7.44.1 | shop_urls 딕셔너리/리스트 불일치로 발굴 크래시 수정 |
| v7.45.0 | 전체 워커 10개를 발굴6/수확1/병합1/검증2로 재배분(경합 분석 후 확정). 수확에도 `ensure_harvest_alive` job 신설(발굴과 동일한 내부 재기동 안전장치, 크론A 백업 필요성 감소). Exa 월예산 샤드당 700회로 재조정(검증 2워커 기준) |
| v7.45.1 | 인라인 python3 -c 블록 3곳의 YAML 들여쓰기 버그 수정(실측 — v7.45.0 배포 직후 발굴 워커 6개 전부 이 버그로 실패) |
| v7.45.2 | merge_discovery_shards job에 pip install 스텝 누락 수정(실측 — export_untranslated.py가 ModuleNotFoundError: pandas로 실패) |
| v7.45.3 | `export_collector_targets.py` 신설 — 로컬 수집기용 검색대상/수집대상/작업대상_통합 자동 생성(화해 우선·블로그성 텍스트 필터링, §1.3의 candidates_summary 오염 사고 재발 방지) |
| v7.46.0 | `ensure_discovery_alive`/`ensure_harvest_alive` 폐기 — §1.1 완전정지 사고의 원인. 외부 수집크론으로 대체 |
| v7.46.1 | 병합 기준선이 유령 샤드(6~9번, 1,829건)까지 세던 버그 수정 — §1.3. 유령 샤드 삭제 + 계산 범위를 워커수(6)로 고정 |
| v7.47.0 | 발굴·수확 재기동시 좀비 실행 자동정리(wake_reset job) 신설 |
| v7.47.1 | wake_reset이 건강한(정상 진행중) 발굴·수확까지 죽이던 버그 수정 — 브랜치 최근 커밋 시각으로 좀비 여부 판단하도록 변경 |
| v7.47.2 | wake_reset이 막 시작한 정상 실행까지 오판하던 구멍 수정 — 취소 후보 실행 자체의 가동 시간도 같이 확인 |
| v7.47.3 | 발굴 워커 무한루프(상점 948/950 정체, 취소도 안 먹힘) 수정 — 무진전 감지+재보충+정상종료 |
| v7.48.1 | 검색어 재보충에 브랜드명 후보 추가(상품명만으론 폐회로라 신규 0개였음) |
| v7.48.2 | 정체시 재보충이 threshold 조건에 막혀 한 번도 안 걸리던 버그 수정, 새 후보를 큐 앞쪽에 삽입 |
| v7.48.3 | shop_related_probe 신설 — 큐텐 상점페이지에 다른 상점 링크가 있는지 조사(결과: **없음** 확인, 이 경로 폐기) |
| v7.48.4 | **발굴 정체의 진짜 원인** — 입력 기본값 500 때문에 상품 500건 도달 시 검색 시도 자체를 0회로 끝내던 버그 수정 |
| v7.48.5 | 수집기 대상 생성이 합의부족·미검증 건까지 넣던 것 수정 |
| v7.48.6 | 검증 오매칭 — 짧은 토큰 부분문자열 오탐("hyaluronic"이 "on"을 포함한다고 판정) 수정 |
| v7.48.7 | 검증 오매칭 — "화장품" 같은 최상위 카테고리 단어가 특이토큰으로 잡히던 것 수정 |
| v7.48.8 | 보완 잔여 건수를 정확히 세도록 수집 시도 기록(page_collect_attempted_at) 추가 |
| v7.48.9 | 번역요청서에 브랜드 포함 — `상품번호\|[브랜드] 일본어명` 형식 |

**가장 중요한 배경 사실**: 네이버쇼핑 검색 API가 **2026-07-31 영구
종료**됐다(NAVER API HUB 이관 대상에서 제외, 대체 없음). 구매링크 확보
경로 전체가 이때 재설계됐다 — 웹문서 검색(대량, 오픈마켓 위주) + 로컬
크롬 검색(정밀, 스마트스토어 전용)의 이중 구조가 됐다.

---

## 3. 현재 검수 상태 (v7.44.1, 1,077건)

| 등급 | 건수 | 뜻 |
|---|---:|---|
| A 완전일치 | 214 | 브랜드·제형·이름·용량 모두 맞음 |
| B 용량·수량 다름 | 221 | 셋은 맞고 용량만 다름 |
| C 이름 비슷 | 208 | 브랜드·제형은 맞고 이름이 정확히 같지는 않음 |
| D 확인 불가 | 242 | 브랜드나 제형을 확인 못 함 |
| S 세트·기획 | 192 | 여러 제품이 묶인 구성 |

**전수검사 진행 상황**: A_01·A_02·A_03(A등급 226건) 육안 대조 완료,
오매칭 2건 발견해 제외 처리(VT 리들샷 스칼프 에센스, 클리니크 차콜
클렌징밤). **A_02 재검사에서 오매칭 2건이 아직 미처리로 남아 있다**
(벨리프 슈퍼나이츠 키트 vs 본품, VT PDRN 리들샷 선택형 상품 —
직전 대화의 마지막 논의 주제, 결론 안 남).

B·C·D·S 등급은 아직 전수검사 전이다.

---

## 4. 관련 스킬 2개 (이름 나오면 자동으로 열림)

- **`qoo10-page-collector`** — 로컬 판매페이지 수집기(현재 v2.4.1).
  검색 모드(링크 없을 때 네이버 검색으로 스마트스토어 찾기) + 수집
  모드(링크 있을 때 실제 판매명·가격 수집). 과거 실패 22건 정리돼 있음.
  **다음 버전 아이디어**: 재고 확인 모드(v2.4.0 계획, 아직 미작성) —
  Actions 의 stock_checker.py 가 스마트스토어를 못 열어서(429) 로컬로
  대신 확인하는 세 번째 모드
- **`qoo10-translation`** — 일본어 상품명 번역 규칙·과거 오역 사례 10건·
  제형 대응표. 번역이 검색어로 그대로 쓰인다는 게 핵심 원칙

---

## 5. 대화 방식 (지켜야 할 것)

- **한 번에 하나씩.** 선택지를 여러 개 늘어놓지 않는다
- 되묻지 말고 진행하라는 지시가 나오면 즉시 따른다(가끔 나옴)
- 규칙을 넓히기 전에 **반드시 영향 건수를 측정**한다
- 오매칭을 발견하면 → 원인 → 대책 → **코드 반영 여부를 물어본다**
- 전문 용어(파일명·필드명 등)를 그대로 쓰지 말고, 필요하면 사람이
  알아들을 수 있게 풀어서 설명한다(직전 대화에서 명확히 지적받음)
- 토큰/비용에 민감하다 — 불필요하게 로그 전체를 반복해서 받아오거나
  같은 브랜치를 여러 번 클론하는 걸 피할 것

---

## 6. 파이프라인 순서

```
① 발굴     검색어로 상점을 찾아 들어가 상품 수집    discovery-live (워커 6)
             -> visited_shops(방문한 상점 목록)가 쌓인다
② 수확     ①에서 방문한 그 상점들을 다시 훑는다     fullcatalog-live (워커 1)
             발굴은 상위 랭크만 봤고, 수확은 전 상품을 본다
             (색조 제외, 리뷰 20 이하). 겹치면 발굴을 우선한다
③ 통합     500건 쌓이면 자동 병합(중복 제거)
④ 번역요청서 생성  export_translation_request.py -> 번역요청_NN.md
⑤ 번역 의뢰   사장님이 다른 창에 붙여넣기 (스킬 qoo10-translation 참고)
⑥ 번역 반영   import_translation_response.py -> translated_kr
⑦ 검증     소스 5곳에서 판매처 찾기(2곳 합의 필수)  hwahae-live (워커 2)
             화해·무신사·다음웹검색·네이버웹문서·Exa (네이버쇼핑 종료)
⑧ 보완     로컬 크롬으로 링크·판매페이지 채우기     사장님 PC
             (스킬 qoo10-page-collector 참고)
⑨ 검수페이지 생성  A/B/C/S/D 등급 분리              main/docs
⑩ 검수     사진 대조 -> 확정/제외                   comparison/decisions
```

**발굴과 수확은 서로 물고 돈다.** 발굴이 상점을 찾아주면 수확이 그
상점을 마저 긁고, 발굴의 검색어가 고갈되면 수확본의 상품명에서 새
검색어를 뽑아 발굴을 이어간다.

**사장님이 직접 봐줘야 하는 곳은 ④번역, ⑧보완, ⑩검수 세 곳이다.**
나머지는 크론으로 자동으로 돈다(§1의 미결 크론 등록이 완료되면 전부).
